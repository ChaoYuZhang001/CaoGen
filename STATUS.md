# CaoGen 项目状态

> 更新:2026-08-10· 实测口径,非文档自评。此文件为活文档,Current Focus 随日更新。
>
> ⚠️ **v0.1.7 已发布，但当前公开 Release 完整性失败。** [GitHub Release](https://github.com/ChaoYuZhang001/CaoGen/releases/tag/v0.1.7) 于 2026-07-25 首次发布时，annotated tag 指向 `d8e883a21b64133b4ec18d20d0c77fd33c054718`，批准的五项 Intel x64 资产名称、大小与 SHA-256 digest 审计全部通过，`caogen.dev` 中英文首页与文档入口已同步。2026-07-26 远端随后增加 `CaoGen-Setup-0.1.7.exe`、其 blockmap 与 `latest.yml`，正文也从仓库内 macOS Intel 五资产合同改成八资产/Windows unsigned 声明；当前 Release 因此不再匹配 `d8e883a2:docs/RELEASE-NOTES-FINAL.md` 所保存的 v0.1.7 五资产合同，不能继续宣称公开资产审计为通过。原五项 macOS digest 未变，候选 `main@bbec526554aea9785291edf4d8164084145347ae` 的签名、公证、staple、Gatekeeper、DMG 隔离安装与真实 renderer 证据仍有效；Apple Silicon 与 Windows 不属于这次历史 Intel-only 发布的批准范围。
> `0.1.8` Intel-only 历史未发布候选 `main@03c3fee2837d120fce43f4b7d11bd25488be4d36` / run [`30243108279`](https://github.com/ChaoYuZhang001/CaoGen/actions/runs/30243108279) 的签名、公证、安装、renderer、五资产与独立下载核验仍只作为历史证据；后续产品源码已继续前进。最新 clean 产品源码证据绑定 `1675eb506eff99b55699bc3a2f5f88b99b5d5ff4`：Deep `test-results/caogen-deep/2026-07-28T05-20-06-681Z/deep-test-report.json` 为 `173 total / 171 required pass / 2 optional skip / 0 blocked / 0 fail`，起止均为同一 SHA、工作树 clean 且 Git 状态未变。上一份该 clean 基线的 Release Doctor `2026-07-28T03-50-46-953Z` 总体仍为 `not_ready`；P2、packaging、release notes、公开 Release 资产与匹配签名候选均未闭合，也没有 v0.1.8 发布授权或公开 tag/Release。
> ⚠️ **正式跨平台 Release Gate 继续 blocked。** 较新的平台限定策略要求 macOS 候选完成 Developer ID 正式签名、notarization、staple、Gatekeeper 与原生启动审计；Windows 只允许文件名和说明均明确标注、且通过原生安装/启动/卸载审计的 `unsigned preview`，它不计入正式矩阵。正式 Windows Authenticode 与其余 12 资产矩阵证据缺失，Release Doctor 仍为 `not_ready`。7 月 31 日 autostash 中的功能与门禁结果均绑定 dirty worktree，只证明局部实现或回归，不是 clean candidate、公开发布授权或 release-ready 证据。
> 当前结构投影为：PRD 64 个 P0 = 21 个已验证 + 30 个部分完成 + 12 个立项目标 + 1 个仅达到基础。它不是版本完成率；最终口径须在冲突清理后的精确 clean 提交上重新绑定 `docs/1.0-ACCEPTANCE-MATRIX.md`。
>
> **状态纪律**(修正第 2 次犯的"未复现即声称"):凡真对话/可用性类结论必须写明**成立条件与复现环境**,不写环境无关的绝对断言。

# Context

国产原创**多厂商 AI 工作桌面**(Electron + React + react-three-fiber,AGPL-3.0-only 开源并提供独立商业授权,[GitHub](https://github.com/ChaoYuZhang001/CaoGen))。差异化站位:**不绑定厂商** —— 支持多模型、多密钥、多厂商配置,接入中转站和本地兼容服务;每个项目可独立配置 AI 工作规则;内置代码执行、项目理解、任务拆解、自动调度、工作区隔离、插件扩展、项目记忆、文件预览和 3D 办公可视化。

# Current Status

- **[v0.1.7 macOS Intel x64 已发布](https://github.com/ChaoYuZhang001/CaoGen/releases/tag/v0.1.7)**(2026-07-25)——首次发布时五项 DMG、ZIP、blockmap 和更新元数据资产公开且 digest 审计 `5/5` 通过；主二进制为 `x86_64`，完成 Developer ID 签名、Apple 公证/staple、Gatekeeper、隔离安装和真实 renderer 启动验证。该历史通过状态已被 2026-07-26 的远端资产/正文追加回归打破，当前公开 Release 审计为失败。
- Release 完整性回归的实时证据：Windows Actions run [`30192957144`](https://github.com/ChaoYuZhang001/CaoGen/actions/runs/30192957144) 只有 `contents: read`，仅上传 Actions artifact，并未修改 Release；正式 Release 的 3 个 Windows 资产在该 run 完成后单独由仓库账号上传。v0.1.7 历史完整性审计以 `d8e883a2:docs/RELEASE-NOTES-FINAL.md` 保存的批准正文为合同，精确比较仓库最终正文、资产名称集合和每项 GitHub SHA-256 digest；当前失败包括正文不匹配、资产数 `8 != 5` 及两个未批准 Windows 文件名。删除资产或恢复远端正文属于外部发布变更，需创始人明确授权后执行。与当前暂停 Windows 范围冲突、试图记录旧 v0.1.6 Windows 八资产文档的 [PR #6](https://github.com/ChaoYuZhang001/CaoGen/pull/6) 已作为 obsolete 关闭并保留分支，不会误合入 `main`。
- v0.1.5 Windows x64 安装包继续保留；v0.1.7 不发布 Windows、macOS arm64 或 Linux 资产。
- v0.1.5 新增整页设置与 Provider 编辑、项目级会话收纳、未关联项目会话收纳、三种显式调度范围，以及调研/策划/开发/测试/文档的默认与自定义模型调度。
- `main@bbec526554aea9785291edf4d8164084145347ae` 的 Intel-only GitHub 最终候选 run [`30162696430`](https://github.com/ChaoYuZhang001/CaoGen/actions/runs/30162696430) 已成功：release-scope P2 gate 通过，exact-commit Deep 为 `159 total / 157 required pass / 2 optional skip / 0 blocked / 0 fail`，x64 完成 Developer ID 签名、公证、staple、Gatekeeper、`120/120` required release audit、DMG 隔离安装、干净 detach、真实 renderer 启动，以及 `latest-mac.yml` 对 x64 DMG/ZIP 的名称、版本、大小和 SHA-512 绑定。该 run 的 Apple Silicon、Windows 与完整矩阵汇总 job 均按输入范围跳过，因此只证明 Intel，不证明完整三平台。
- 仓库保留完整三平台矩阵门禁，只有选择完整矩阵范围或宣称三平台发布时才要求 macOS x64、macOS arm64、Windows x64 全部通过 required distribution audit、包内 clean-commit provenance、目标平台原生安装和真实 renderer 启动。2026-07-25 的 v0.1.7 M1 发布范围当时收窄为 Intel-only；该历史范围使用 x64 lane 的独立签名/公证/安装审计，不把跳过的 arm64/Windows 算成 pass。完整矩阵 Release Doctor 始终保持 `not_ready`；后续允许 Windows unsigned preview 的平台限定策略不改变这一正式门禁。
- `.github/workflows/release-candidate-evidence.yml` 已把 M1-T3 固化为只读、手动、不可发布的候选证据管线：输入必须是已在 `main` 的完整 40 位 SHA 与精确版本；可选择 `macos-x64` 或完整三平台范围。run `30162696430` 已证明仓库中的 Intel 签名/公证 secrets 可用；这不证明 Apple Silicon/Windows secrets 或资产。workflow 不创建 tag/GitHub Release。
- 移除 Claude Code Runtime 前的本地 unsigned Intel 基线为 DMG `197,518,789 B`、ZIP `197,966,775 B`、`.app` 逻辑体积 `555,506,427 B`、`app.asar` `61,742,511 B`，其中 Claude CLI 为 `240,192,080 B`。移除后的本地 unsigned Intel 实测为 DMG `125,638,991 B`、ZIP `125,002,021 B`、`.app` 逻辑体积 `311,931,918 B`、`app.asar` `61,630,882 B`，分别减少 `36.39% / 36.86% / 43.85% / 0.18%`。该 v0.1.7 clean candidate 的签名公证资产为 DMG `127,702,230 B`（SHA-256 `a6b65ddd7d11bc8aab36cd800a7ddd9055b562d5aa85b39ef0296fb9c4f78a7b`）与 ZIP `127,016,670 B`（SHA-256 `5f3695fa24117145cd47ecf731660ea3423911ad7bc2de9108c3f786aa78f5e0`）；包含两项 blockmap 与 `latest-mac.yml` 的五文件资产集绑定 `7553d1ef33ec44d69e7b95c74aee8fcb7500a68daf008ed343e66ae3345a036c`。required audit 证明 SDK/CLI 在该历史包内不存在，且 app、DMG、ZIP 的 `0.1.7`、clean worktree 与完整提交 provenance 一致。
- M1-T4 在 v0.1.7 发布时的仓库公开进度快照为 `64 = 21 已验证 + 19 部分完成 + 23 立项目标 + 1 仅达到基础`。官网生产构建同步了 v0.1.7 下载、Intel-only、签名公证和运行时边界；该历史快照不改变 Release Doctor 的 `not_ready` 状态，也不替代后续平台、真实 Provider、N1 或完整 1.0 验收证据。
- 已发布 v0.1.7 Intel 包只有 OpenAI-compatible（Responses / Chat Completions）与原生 Anthropic Messages（`/v1/messages`）两条执行路径；Claude Code Agent Runtime、Claude Agent SDK 与 Claude CLI 不在该基础发行包内。该 Intel 候选 Deep 报告 `2026-07-25T15-06-42-357Z` 为 `159 total / 157 required pass / 2 optional skip / 0 blocked / 0 fail`，开始和结束均绑定 clean `bbec5265`；它只证明该精确提交和 Intel 候选，不约束后续源码中显式可选的 Claude 专项，也不替代真实 Provider、N1 或完整 1.0 验收证据。
- M1-T6 的原始发布动作已完成：只读、fail-closed 的 Intel 发布交接预检在 publication-only 后继提交 `d8e883a2` 上通过后，创始人授权创建 `v0.1.7` Tag/Release 并上传精确五项资产；当时公开资产审计为 `passed`。2026-07-26 的远端追加已使该发布后断言失效；仓库现增加每 6 小时及手动运行的只读 Release 完整性审计，并把 Windows unsigned 构建输出改成显式 `unsigned-preview` Actions artifact、排除 `latest.yml`、要求手动确认 preview-only。当前还需获得明确授权后恢复远端五资产/正文合同；该回归不关闭陌生用户 Quick Start、N1、真实 Provider 或 1.0 验收。
- M1 首位陌生用户验收工具链已就绪：`docs/M1-FIRST-USER-DRILL.md` 固定官网→Intel DMG→无安全绕过安装→Provider 配置→只读任务五步；私有结果 schema v2 和 `test:m1-first-user-onboarding:required` 绑定 `v0.1.7`、候选提交、公开 DMG SHA-256、Intel 架构、30 分钟、零修改及四份独立证据，并强制录屏前明确同意、唯一证据用途、最长 30 天保留、`deleteBy`、脱敏复核和真实删除状态。文件仍存在或审计未完成时不得声称已删除。失败演练可用 `--observation` 留下 `observed_failed`，但不会成为通过；负向 smoke 已覆盖原有发布/通过条件以及无同意、未复核、超期保留和虚假删除。当前仍没有非项目参与者真人结果，M1 整体完成判据保持开放。
- M1 招募已公开：`npm run prepare:m1-first-user-drill` 会在仓库外创建权限收紧的私有目录、记录模板和主持人清单，不下载 DMG、不创建假证据；中英文 README、[Discussion #9](https://github.com/ChaoYuZhang001/CaoGen/discussions/9) 和 `CaoGen-Website@f8e8c50` 生产下载区只征集 Intel 机型、时区和可参与时间，并明确禁止公开 Key、Provider URL、项目路径、录屏或证据。Discussion 已增加证据生命周期与候补名单规则，并从 General 原地移到 Announcements；GitHub 仓库 About 的空 homepage 字段已补为 `https://caogen.dev`，不修改或覆盖 v0.1.7 Release。首条外部评论是验收设计建议，不是报名；它暴露的 SLA 漏算已在 `main@b98acd11` 修复，live audit 现将其作为独立线程并记录 `1 responded_on_time / 0 pending / 0 overdue / 0 late`，维护者首次回复耗时 51 分钟。2026-07-27 只读复查仍只有该 1 条外部设计建议，open Issue/PR 均为 0；当前合格参与者和真人记录仍为 0，不能关闭 M1。
- M1 招募增量已在 clean `main@d424d6c2be8ce4b0c0b3237d7255a9740495a3c1` 完成全量 Deep：时间戳报告 `test-results/caogen-deep/2026-07-26T03-24-20-655Z/deep-test-report.json` 为 `163 total / 161 required pass / 2 optional skip / 0 blocked / 0 fail`，开始和结束都绑定该提交、clean 且 Git 状态未变；新增私有准备 smoke 与原有首位用户审计 smoke 均为 required pass。该结果证明仓库回归稳定，不是陌生用户安装、Provider 配置或任务完成证据。
- 社区 SLA 修复与 M1 schema v2 证据治理合并后，clean `main@0874f5a71f890403c008100889c5eb339eb57f98` 的完整 Deep 报告 `test-results/caogen-deep/2026-07-26T06-11-06-988Z/deep-test-report.json` 仍为 `163 total / 161 required pass / 2 optional skip / 0 blocked / 0 fail`；起止均绑定该提交、clean 且 Git 状态未变。两项 M1 smoke、社区契约与 SLA smoke、typecheck、build、编码标准及共享 Electron/恢复链均为 required pass；两项中国真实外部检查继续是 optional skip，未计作通过。远端由维护者回复触发的 SLA Actions run [`30190540484`](https://github.com/ChaoYuZhang001/CaoGen/actions/runs/30190540484) 也在含修复的 `b98acd11` 上成功。该工程证据不替代陌生用户真人结果。
- M2-T2 onboarding 应用内基础已在 v0.1.7 后续源码继续收口：欢迎页首张卡精确注入公开 `quick_start_project_read_only_v1` 只读提示词；Assistant 无执行资源与 Expert 无 Provider 都提供可操作恢复。恢复动作现在携带首任务来源，无可用 Provider 时直接进入新增/修复 Provider 表单；恢复场景缺 API Key 或缺模型时会留在表单内给出双语明确错误，且不会创建半成品 Provider，普通设置场景仍可分步配置。只有保存出“已配置密钥且至少一个模型”的 Provider 才自动返回首任务，取消或不可用保存不会误关闭设置。首任务提示词、目录、Drive、权限、路由模式、Provider/模型和 Studio 子页均跨设置往返保留，恢复成功后旧错误自动消失且发送前仍为零会话。真实 Electron `routing-zero-choice` E2E `2026-07-26T14-44-18-900Z` 为 `9/9`、6 张截图，已通过真实 UI 验证缺 Key、缺模型、有效保存三段路径，不再用 IPC 直接创建 Provider；随后仍走真实 Router/流式响应及模式切换一致性。编码标准 ratchet 把 `WelcomeView` 从 296 行/复杂度 18 降到 222 行/复杂度 11，未新增硬债务。此前脏工作树完整 Deep `2026-07-25T18-20-19-937Z` 为 `160 total / 158 required pass / 2 optional skip / 0 blocked / 0 fail`；该结果和本次 targeted E2E 都不代表 clean release 绑定、M1 真人验收或 M2-T2 完成，仍需用首位陌生用户和三人 N1 记录验证配置成功率、文案理解与恢复时长。
- 首轮 clean Deep `2026-07-26T14-56-32-490Z` 在 required page operations 尾段真实发现：关闭最后会话并删除项目后，Welcome 草稿未固化推导出的项目选择，项目消失时会退回“新项目目录”，下一次发送会静默重建已删项目。功能候选 `7ed1b5fb4e7d414587734f8c660aca8b8c40bad9` 已把首个项目选择写入草稿，并在项目删除或归档后同步投影为“未关联项目”；最终 targeted page report `2026-07-26T15-39-17-535Z` 与完整 Deep 子报告 `2026-07-26T16-11-50-301Z` 均为 `22/22`。完整 Deep 报告 `test-results/caogen-deep/2026-07-26T15-42-01-402Z/deep-test-report.json` 为 `163 total / 161 required pass / 2 optional skip / 0 blocked / 0 fail`，起止均精确绑定该 SHA、clean 且 Git 状态未变。该结果证明现有 onboarding/恢复链回归稳定，不关闭真人 M1、公开 Release 完整性或 formal 1.0 门禁。
- 2026-07-28 的主线历史决策是：v0.1.7 tag 后已有用户可见修复进入 `main`，但公开安装包不包含它们，因此当时按 `docs/PLAN.md` 的 patch 规则维护 `0.1.8` macOS Intel-only 补丁线；版本与候选合同锁到 `0.1.8` / `macos-x64`，手动证据 workflow 保持 `contents: read`，Apple Silicon/Windows 暂停，v0.1.7 远端正文与 3 个 Windows 资产不修改。该决策只界定下述历史 run，不是 7 月 31 日 autostash 增量的最终发布策略；当前必须先解决主线 `0.1.8` 与 autostash `0.1.7` 之间的 release identity，再决定最终版本和 macOS arm64 / Windows unsigned preview 范围。下述 run 只关闭了 `03c3fee2` 的候选构建、独立下载、Final Notes 与 clean scoped notes/handoff preflight；后续产品源码已经前进并完成新一轮 clean Deep，该候选已降为历史证据，不再处于 `ready_for_owner_decision`。
- `main@03c3fee2837d120fce43f4b7d11bd25488be4d36` 的 0.1.8 Intel-only 候选 run [`30243108279`](https://github.com/ChaoYuZhang001/CaoGen/actions/runs/30243108279) 已成功：source gates 与 P2 通过，exact-commit Deep 为 `163 total / 161 required pass / 2 optional skip / 0 blocked / 0 fail`，macOS audit 为 `120/120`，Developer ID 签名、应用公证/staple、Gatekeeper、DMG 隔离安装/干净 detach、真实 renderer 和 packaged-app smoke 全部通过。五资产集 digest 为 `2abe8622e3b37873e69abdd5deb1f16c8739336181688eeb2e665c601792ff52`；Apple Silicon、Windows 和完整矩阵汇总均按范围跳过。Actions artifact `8644829708` / `caogen-release-macos-x64-03c3fee2837d120fce43f4b7d11bd25488be4d36` 到期日为 2026-08-10；其 `255,552,267 B` ZIP 已用可续传分段下载、按精确偏移重组并复算 SHA-256 `ec343fe823c5e3a3502c4b6176d23dd59b46dd3e15c2338b06eab98f7384c16a`，ZIP CRC、四类报告、五项资产大小/SHA-256、`latest-mac.yml` SHA-512、provenance、纯 x86_64、deep/strict codesign、Gatekeeper、应用票据及 Claude SDK/CLI 缺失均独立复核一致。DMG 文件本体没有 stapled ticket，现行合同与 `120/120` audit 验证的是归档内应用票据；文档不冒充 DMG 本体已 staple。`1707a999 / 30235833246` 降为历史证据；本组不授权 tag、Release、v0.1.7 远端修改或关闭真人 M1/formal 1.0 门禁。
- 等待测试者期间已关闭首任务恢复缺口：欢迎页草稿以版本化本地记录保存提示词、项目选择、目录、Drive、权限、路由、Provider ID 与模型，renderer reload 或应用重启后可恢复；成功创建首个任务后自动删除。启动时 Project/Provider 目录尚未完成异步加载，不再把空列表误判为资源已删除；目录加载完成后，真正删除、无密钥或无模型的 Provider 会 fail-closed 解绑，固定模型失效会清空模型，不会静默切换到其他 Provider。用户可从真实 Provider 编辑器重建配置，提示词、项目和其余草稿字段保持不变。记录不包含 API Key、Provider Base URL 或模型响应，损坏/超限记录会丢弃，存储不可用时退化为原内存草稿。真实 Electron `routing-zero-choice` 报告 `2026-07-26T18-41-31-736Z` 为 `11/11`、8 张截图；页面操作回归 `2026-07-26T18-42-00-814Z` 为 `22/22`，并已纳入上述 clean 候选的完整 Deep 与签名分发证据。该工程证据仍不替代真人 M1。
- 等待测试者期间修复固定模型与 Drive 切换冲突：Studio 首任务已明确选择有效 Provider/模型时，切换 Drive 只更新 Drive 与默认权限，不再把固定模型改成自动路由并留下空模型；若模型确实已从 Provider 目录消失，仍 fail-closed 清空。真实 Electron `routing-zero-choice` 报告 `2026-07-26T20-07-47-489Z` 为 `12/12`、9 张截图，页面操作 `2026-07-26T20-08-40-109Z` 为 `22/22`；该修复已进入 `03c3fee2 / 30243108279` 的 clean Deep、签名、公证、安装与 renderer 候选证据。
- 等待测试者期间修复运行中重复发送的草稿丢失与假排队：Composer 现在允许继续起草，但任务处于 `starting/running` 时禁用发送并明确提示任务完成后再发；Browser 批注和产物 Preview 的发送入口也遵守同一忙碌门禁。只有主进程确认接受消息后才清空文本和附件；被执行引擎拒绝的消息会撤回乐观记录、恢复原会话状态并保留草稿，IPC 与 IDE Bridge 返回真实 `boolean`，记忆建议/自动提取只在确认接受后触发。真实 Electron live-switch 报告 `2026-07-27T03-28-37-915Z` 为 `6/6`，页面操作报告 `2026-07-27T03-26-54-546Z` 为 `22/22`；候选 run `30243108279` 已把该修复绑定到 clean `03c3fee2`。工程与候选通过仍不替代真人 M1。
- 等待测试者期间继续修复 Browser 批注假成功：发送不再吞掉执行引擎拒绝，而是显示发送中/成功/失败；共享入口只允许 `idle/error` 会话，`starting/running/closed/undefined` 均不可发送，Preview 同步收紧。真实 Electron 页面操作 `2026-07-27T05-08-28-234Z` 为 `22/22`，Browser 成功回执截图已人工复核无溢出或重叠；Preview required smoke 锁定完整状态矩阵，build、typecheck 与编码标准 required 通过，`BrowserPanel` 主函数由 184 行/复杂度 15 降至 170 行/复杂度 11。该修复已由本地 clean Deep `2026-07-27T05-57-32-765Z` 和候选 run `30243108279` 绑定到 `03c3fee2`。
- 等待测试者期间继续修复其他发送入口的假成功：IDE Bridge `sessions.create` 现在等待首条消息并通过 `initialMessageAccepted` 明确报告接受/拒绝，VS Code 不再把拒绝提示成 ready；Routine 只有在提示词被 SessionManager 接受后才记成功，拒绝时记失败并关闭空会话；开工建议拒绝时保留建议并显示错误，发送等待期间切换会话也不会把成功/失败写到错误会话。IDE 协议负向 smoke、Routine 组合负向 smoke、开工建议并发行为 smoke、P1/default P2、真实 VS Code Extension Host、Assistant/Studio live-switch、build、typecheck 与编码标准 required 已通过。完整 P2 release-scope 在本机仍按设计失败：缺 Gradle/JetBrains 插件产物和真人 IDE 交互证据；VS Code 编译与真实 Host smoke 已通过。clean `main@3229c7af0ed05fd99f82c79ce5e7fd81f04e3506` 的 Deep 报告 `test-results/caogen-deep/2026-07-27T08-45-57-258Z/deep-test-report.json` 为 `165 total / 163 required pass / 2 optional skip / 0 blocked / 0 fail`，起止均绑定同一提交、工作树 clean 且 Git 状态未变；新增两条负向 smoke 均为 required pass。该结果不等于 JetBrains release-scope 证据或签名候选，`03c3fee2 / 30243108279` 仍只保留为历史证据。
- 等待测试者期间修复快照恢复多步骤静默拒绝：旧实现会在同一循环连续发送所有未完成 prompt，首步把引擎切到 `running` 后，后续步骤被忙碌门禁拒绝但结果未被处理。现在自动恢复与 Supervisor 显式恢复共用顺序协调器，一次只发送一步，上一轮成功且回到 `idle`、ModelAttempt 门禁刷新完成后才继续；恢复期间普通发送不能插队，任一步执行失败、同步/异步拒绝、门禁刷新失败或会话关闭都会停止后续自动续跑并留下明确事件。新增 smoke 覆盖三步顺序、重复 idle、首步/后续拒绝、同步引擎错误、原始异常保留、恢复门禁失败、关闭清理和普通发送门禁，并已加入 `test:task-run` 与 Deep required 清单。clean `main@3fb95befa8566bfd90890a51abe279350b3225f7` 的 Deep 报告 `test-results/caogen-deep/2026-07-27T14-37-45-555Z/deep-test-report.json` 为 `166 total / 164 required pass / 2 optional skip / 0 blocked / 0 fail`，起止均绑定该提交、工作树 clean 且 Git 状态未变。该结果仍不等于签名候选、真人 M1 或 formal 1.0 验收。
- 等待测试者期间修复自由子代理编排的静默拒绝：旧实现忽略 child 首条任务和父汇总的 `send()` 结果，child 被门禁拒绝后会永久留在 pending，父汇总被拒绝时却会先删除编排状态并丢失全部结果。现在独立协调器把被拒 child 立即记为失败并向父会话发出 `subagent-result`/hook；父会话忙碌时等待，明确拒绝时保留同一份汇总，只有后续 `idle/error` 恢复事件才重试，已经被接受的汇总不会自动重复发送。负向 smoke 覆盖正常 fan-in、child 拒绝不挂起、父会话忙碌延迟、父汇总拒绝后同文重试和父会话关闭清理，并已加入 `test:task-run` 与 Deep required；真实 Electron 编排 E2E 同时通过。代码标准 ratchet 把 `sessionManager.ts` 从 1851 行降到 1798 行、`dispatchSubagents` 复杂度从 20 降到 19。clean `main@b2f967d6b642c79877bd5a3ce1ac83b402608368` 的 Deep 报告 `test-results/caogen-deep/2026-07-27T15-51-27-833Z/deep-test-report.json` 为 `167 total / 165 required pass / 2 optional skip / 0 blocked / 0 fail`，起止均为该提交、工作树 clean 且 Git 状态未变。该结果仍不等于签名候选、真人 M1 或 formal 1.0 验收。
- 等待测试者期间修复模型交叉验证的静默拒绝：旧实现创建第二模型复核/第三模型仲裁会话后忽略首条 `send()` 结果，即使 SessionManager 拒绝也显示“已启动”，并永久保留无法完成的 child 关联。现在发送接受边界会捕获同步异常与明确拒绝，清理对应 review/arbitration 关联并在父会话留下原因；只有首条指令被接受后才显示启动事件。专项 smoke 覆盖接受、拒绝和同步异常，完整 P2、build、typecheck 与编码标准 required 通过。clean `main@85daf906a917cf1ab44a5f2ce5586e63c4072763` 的 Deep 报告 `test-results/caogen-deep/2026-07-27T16-31-50-392Z/deep-test-report.json` 为 `167 total / 165 required pass / 2 optional skip / 0 blocked / 0 fail`，起止均为该提交、工作树 clean 且 Git 状态未变。该结果仍不等于签名候选、真人 M1 或 formal 1.0 验收。
- 等待测试者期间把高风险 Effect 入口清单推进到 inventory v8：TypeScript AST 精确锁定 156 个 Electron IPC、52 个 Agent 工具和 79 个 ProjectWorkspace/DigitalWorker/Supervisor 嵌套动作；IPC policy 为 13 个 queryable、13 个 opaque、7 个 direct-user 和 6 个 delegated。既有附件、项目规则、MCP probe、managed-plugin、终端与 Browser Effect 边界不变；`migration:import` 新进入 opaque `migration_import` Effect，实际导入前必须持久化 `executing`，Ledger 只保存资产数量、类型计数与选择摘要，不保存源资产路径、源内容或原始错误。失败或 SIGKILL 后保持 `waiting_reconciliation`、刷新 Renderer 恢复面板并禁止自动重放，人工确认未应用后可收敛并释放同资源 lease。clean `c9e9c2a2a001ad0a6aac7f1c202d48aa0992f501` 的 Deep 报告 `test-results/caogen-deep/2026-07-28T07-08-00-218Z/deep-test-report.json` 为 `174 total / 172 required pass / 2 optional skip / 0 blocked / 0 fail`，起止均绑定同一 SHA、工作树 clean 且 Git 状态未变；migration crash targeted E2E 已纳入 required。完整迁移预检、备份、跨文件原子回滚和幂等重跑仍未闭合 `NFR-REC-005`，其余 7 个 direct-user 外部 IPC 仍未进入 Effect Ledger，因此 `TRUST-002` 继续只算部分完成，`TRUST-003/004`、签名候选、真人 M1 和 formal 1.0 验收仍开放。
- Assistant/Studio 性能门禁在 M2-T3 集成时连续三次真实失败，失败点分别为 mobile warm `307ms`、tablet cold `393.4ms`、mobile warm `433.2ms`，均按失败保留。诊断确认 Studio chunk 首次点击才加载、已挂载的隐藏 Project/DigitalWorker 子树会随模式切换重复渲染，且宿主偶发 animation-frame 调度空洞会污染 wall-clock；现在使用可重试模块 Promise 后台预载、memoize 无模式 Context 依赖的 Studio 根节点，并要求每个正式样本前先建立 4 个连续 `<=50ms` 健康帧，5 秒内无法建立则 required 失败。完整脏工作树 Deep `2026-07-25T21-09-18-544Z` 为 `162 total / 160 required pass / 2 optional skip / 0 blocked / 0 fail`，同一 `3 cold + 60 warm`、原 `<300ms` 门禁 cold P95 `32.90ms`、warm P95 `32.60ms`；帧健康前置真实捕获到最长 `1908ms` 宿主调度空洞，正式样本最大值 `33.80ms`。live-switch `5/5`、canonical consistency `5/5`、required UI `9/9` 和 page operations 同时通过；该结果来自 dirty worktree，不冒充 clean release 绑定。
- Studio 新会话状态已按投影语义收口：`showNewSession` 会把 Studio 切到自己的会话面板，侧栏/菜单新建保留用户当前模式，3D Office 新建离开 Office 后显示可编辑 Composer，不再留下隐藏 Welcome DOM。macOS Tray 也从每 5 秒无条件重建菜单改为订阅 Session 事件，并只在 running 数量变化时更新；完整 Deep 内 page operations、Assistant/Studio required UI 和 macOS Tray icon smoke 均通过。
- M2-T3 反馈通道已完成首个真实闭环：公开 GitHub Discussions 保留 General、Ideas、Q&A 分类，仓库提供双语分类表单、Issue/README/SUPPORT 导流和 48 小时维护者首次回应口径；只读工作流每 6 小时及新评论事件扫描公开 Issue、PR、Discussion。SLA 审计现在把维护者 Discussion 下的外部顶层评论作为独立反馈线程，OWNER/MEMBER/COLLABORATOR 的直接回复才计数，自动回复不计数，当前逾期阻断、历史迟复告警。Discussion #9 的外部建议从修复前错误隐藏，变为修复后的 `pending`，并在维护者 51 分钟内回复后成为 `responded_on_time`；当前 `0 pending / 0 overdue / 0 late`。这证明反馈入口与首次响应闭环，不证明该评论者是产品测试者或 M1 已通过。
- M2-T5 官网演示视频已上线：独立 Remotion 工具源码已随 `main@16954790` 提交，官网仓库 `main@e5be292` 已部署到 Cloudflare Pages；中文/英文首页均实测包含 `#demo`、原生 MP4 播放器和移动端单列布局，生产 MP4 返回 `200 video/mp4`，大小 `3,369,296 B`，SHA-256 为 `bdb93198b35db545a6af0fd5530b0eb598eb4c5680085f48e3b88810711a553f`。该项只关闭 M2-T5，不代表 M2、M1 或 1.0 完成。
- Agent 恢复内核已升级为稳定事件身份 + 恢复游标 + 持久 Effect Ledger。`write_file`、`search_replace`、OpenAI/Anthropic 原生文件编辑、Git commit/merge/push、Renderer 文件与 Git Index 操作、文本 hunk 丢弃、managed-worktree create/remove/patch 和 GitHub/GitLab PR/MR 已接入专用只读对账；Operation Gateway、lease/fencing、强杀恢复、DAG autoMerge durable finalizer/receipt 已由 required E2E 覆盖。该边界不等于外部系统事务级 exactly-once，writer 仍可能在强杀/断电/ENOSPC 时留下半写文件。
- 在后续 autostash 增量之前，clean 主线历史基线中的 `task-snapshots.db` 为 v8：保留 v6 TaskRun Effect evidence append-only hash-chain foundation、Goal/WorkItem/Run/Artifact/Acceptance/Evidence Link 与全局 workflow event chain，并新增 canonical `workflow_recovery_sessions` 和持久 `workflow_store_identity`。显式对象的 main API、IPC/preload、Control Center 查询/校验和 cursor 分页已有 targeted smoke；Artifact Graph 的 edge/location、关系/归属校验、邻域查询、脱敏 export 与只读 diagnose/repair plan 也已接通。Task Snapshot/TaskRun 恢复读取支持 `legacy`、`compare`、`canonical` 三态：`legacy` 读取旧 Snapshot/TaskRun 表，`compare` 同时读取 legacy 与 canonical 并在差异时 fail-closed，`canonical` 读取 Workflow Run 与 recovery session；未显式配置时仍默认 `legacy`。read mode 按解析后的数据库路径隔离，跨 mode 首次 open 共享同一路径的 single-flight readiness；运行时 mode flip 在数据库 mutation queue 中强制刷新 readiness，并实际读取 recovery sessions 与 Run 历史后才提交。legacy JSON/旧 SQLite 到 v8 的迁移使用 `prepared -> backup_verified -> migrated_verified -> committed`、`rollback_pending -> rolled_back`、精确备份/SHA-256/fsync/原子 rename、崩溃 checkpoint 续做和可恢复回滚；future/corrupt source 在 journal 前 fail-closed。committed journal 通过 `workflow_store_identity` 和 committed 高水位连续性阻止目标删除、截断、版本回退或同版本有效空库替换。Canonical-only 历史在后续双写和重启后仍可读取；同一 session 的连续 TaskRun、Snapshot freshness 和历史 Run ownership 已按精确 run/项目上下文收口。该能力完成的是该 clean 基线上的 Task Snapshot/TaskRun 恢复读源可验证 cutover，不代表所有业务入口已经 canonical，也不等于完整 1.0 Workflow OS。结构化交叉验证仲裁失败与原生 `bash` 显式测试命令的非零退出失败已有受限入口；Routine、DigitalWorker/Assignment、其他工具/引擎测试结果等外部事件仍未全部接入。完整 Artifact Graph 生命周期（blob/sourceRef、版本/保留/删除）、Canonical Conversation Ledger、统一 retention/delete 和生产补偿计划/审批/执行也未闭环。独立 `task_evidence` 子链没有直接 UI 或统一 retention/delete 通道。该 clean 候选的 159 项 Deep 已包含 migration、read source、shadow consistency、Workflow Ledger、Artifact Graph、security、maintenance、ModelAttempt crash reconciliation、Canonical Goal/WorkItem schema parity、canonical ProjectWorkspace write-source crash、Acceptance failure ingress、Artifact byte integrity、TEAM-002 真实 Electron 招聘、Provider credential target binding、Assistant/Studio live-switch、Anthropic 生产路径、Code Forge contract smoke 与 Intel 发布元数据证据；报告绑定 `bbec5265` 且开始和结束均为 clean。后续 autostash 中的 v9 增量见下文；两者都不等于 1.0 release ready。
- Canonical ModelAttempt v1 已把 OpenAI-compatible 请求、模型 DAG 与原生 Anthropic Messages 每次底层 HTTP 请求接入 Run/WorkItem 归属、逻辑 request/step、Provider/model/protocol、route reason、usage 和不可变事件链；`started` Attempt 在强杀重启后会把 legacy TaskRun、canonical Run 和匹配 snapshot 原子投影为 `waiting_reconciliation`，普通发送/恢复/删除均 fail-closed。用户只能显式 `retry_authorized` 或 `cancelled_by_user`；授权本身不调用 Provider，successor 必须消费同一 requestId/stepId 并链接 predecessor，二次强杀不会复用旧授权。原生 Anthropic Messages 已注册独立 EngineKind 并接入 Provider/SessionManager/UI；saved Provider/Broker 目标绑定、`/v1/messages`、thinking/redacted thinking/text SSE、usage、HTTP/流错误、取消、`tool_use/tool_result` 多轮 NativeToolRuntime、权限/审计/Effect/幂等、40 请求上限、同 Provider Key 与仅限 Anthropic Engine 的 Provider failover，以及内容寻址图片重启恢复，分别由 `test:anthropic-messages:required` 17/17、`test:anthropic-tool-use-loop:required` 10/10、`test:anthropic-failover:required` 8/8、`test:anthropic-engine-registration:required` 和 `test:anthropic-image-restart:required` 覆盖。partial 输出、abort、账本失败或未决 Effect 会保守阻止重放；当前仍缺真实 Provider、clean release 绑定、完整恢复阶梯与统一 Run/Context 契约，因此 ROUTE-004/RUN-003 仍仅为部分完成。
- 以下 2026-07-29 至 2026-07-31 增量来自较新的 autostash/dirty-worktree 证据；它们保留功能实现与局部回归事实，但不覆盖上面的公开发布历史，也不构成 clean release 绑定。
- M1 文档、x64 签名基线与跨平台矩阵门禁已快进到 `main@4a3f6359`；`package.json` 与 lock 根版本均为 `0.1.7`。该 clean commit 上的完整 Deep 为 `158 total / 155 required pass / 3 optional skip / 0 blocked / 0 fail`，报告为 `test-results/caogen-deep/2026-07-23T12-53-59-079Z/deep-test-report.md`；测试开始和结束均为 clean 且 Git 状态不变。新增候选 workflow 增量仍需在合并后的最终 clean commit 重跑 Deep。
- 本轮“macOS 正式 + Windows unsigned preview”策略增量已在 dirty worktree 上完成 `160 total / 157 required pass / 3 optional skip / 0 blocked / 0 fail` 回归，报告为 `test-results/caogen-deep/2026-07-24T04-32-13-214Z/deep-test-report.md`；开始/结束均绑定 `61e895dc` 且 13 个状态项不变。该结果证明本轮改动未造成 Deep 回归，不是 clean release candidate 证据。
- 2026-07-30 开发收口已完成：类型检查、构建、编码规范 required（18 pass / 0 fail）、密钥扫描、私有 Provider 配置/报告脱敏和页面全量 Electron 专项均通过；页面专项修复报告为 `test-results/caogen-deep/2026-07-30T03-46-18-194Z/page-operation-smoke.json`。最终完整 Deep 报告仍以 `test-results/caogen-deep/2026-07-30T02-20-50-755Z/deep-test-report.md` 为准，其中旧页面专项失败已由上述定向回归覆盖，但本轮未重复完整 Deep；以上均为 dirty worktree 证据。Release Doctor 最新 refresh 报告 `test-results/workos-release-doctor/2026-07-31T06-16-08-672Z/report.json` 绑定当前 `b37c17cf8dea` dirty worktree（410 个状态项），仍为 `not_ready`；当前开放域为 evidence refresh、release identity、Deep、packaging、product positioning、release notes 与 GitHub release assets。refresh 中 packaging、macOS distribution 与 PRD 四态 README 快照检查失败；GitHub API 在当前沙箱 DNS 不可达，公开资产审计只能记为 `skipped/open`。正式跨平台和平台范围候选均为 `blocked`，不得宣称正式 release ready。
- 正式跨平台门禁继续要求完整三平台矩阵，而不是把 Windows preview 当成正式证据：macOS x64、macOS arm64、Windows x64 均需独立 required distribution audit、包内 clean-commit provenance、目标平台原生安装和真实 renderer 启动；正式 packaging audit 仍要求 12 项资产，Windows 仍校验应用与安装器 Authenticode/时间戳。Release Doctor 因正式 Windows 签名证据缺失保持 `not_ready`，该要求没有降级。macOS 签名钩子仅对 Apple TSA 瞬时错误做最多 5 次有界重试；完整重试探针因 Electron 下载网络停滞未进入签名阶段，不能计为通过。
- Windows preview 使用独立的 `electron-builder.windows-preview.cjs` 和 `windows-preview-audit`：关闭证书自动发现与强制签名；唯一公开安装包名固定为 `CaoGen-<version>-windows-x64-unsigned-preview.exe`，不生成 `latest.yml`，且 required 审计必须在 Windows x64 确认应用与安装器确实未签名、PE 架构、clean provenance、原生安装、renderer 启动和卸载。preview 通过只证明预览通道可用，不能关闭 `packaging_release`。
- 2026-08-06 当前 FIX-000 D0 Windows x64 `unsigned-preview` 已重建：EXE 220,748,412 bytes，SHA-256 `8da0d31fe2de689dd321a5f72da292a830e7386246cd78015d80c33a0b1b7a44`，artifact-set SHA-256 `04c098f9f16d96fa17cd81b14917557197740574cc495d24cac6b67557c07832`，dirty preview 审计 40/40 通过。当前本机仍运行上一 D0，已安装 EXE SHA-256 为 `38acc48809786e96d3a2445348e5b598b17844bba21769d7266ab020f03863d6`；上一 SHA 的 installed 与 Owner 观察继续作为历史缺陷证据，但不满足当前 SHA。当前 Owner Kit 为 253,989,974 bytes，SHA-256 `eb12a7fd12667f07a70ede967e23367bb83081bcbf821f208654c9ae18175758`，manifest SHA-256 `20a7fbf70decc55747f036f3c64962931c964a9d4976c9d76ae47ab5b52ea9b7`，content-set SHA-256 `9660960cc159dbffe5b9190015899bcbb422fa483d24bbf77f922b37e908371b`；当前 SHA 的替换安装、installed smoke、真实首任务和 12 步 Owner required audit 仍未执行，保持 No-Go。
- 2026-08-06 当前 D0 的首次保留数据替换尝试在 120 秒后失败，安装器显示旧应用文件无法卸载并返回 `: 2`。旧 CaoGen 进程已退出、活动会话为 `idle`；独占锁扫描与 Windows Restart Manager 均确认旧安装的 `resources/app.asar` 被 WorkBuddy 后台进程持有。失败后旧 EXE/卸载器完整，8/8 关键用户状态文件摘要不变，当前 D0 未安装。证据为 `test-results/fix-000-replacement-install/2026-08-06T00-46-40Z/report.json`。关闭 WorkBuddy 需 Owner 操作或明确授权；在此之前替换安装不能继续计时为通过。
- 2026-08-06 外部 WorkBuddy 锁清除后，当前精确 D0 已退出码 0 完成保留数据替换安装，安装 EXE SHA-256 为 `e02013702d1bc778b9314c51ea8b098c371ab2e1c3dedbfeb44251ab5160b6b6`。真实只读首任务使用保存的具体 Provider/model 与 `routingScope=fixed` 的 Anthropic 会话，`list_dir + 2×read_file` 的 3 个工具结果均无错，`turn-result=success`，会话 `idle`；源项目用户 Git 条目 0，隔离 worktree 仅 `.caogen` 审计日志、用户条目 0，10/10 凭据仍为 `enc:`。证据为 `test-results/fix-000-first-task/2026-08-06T-installed/success-summary.json`。当前 SHA 的 S4 首任务路由与只读改动复测已通过；installed smoke、12 步 Owner required audit 和卸载两步仍未完成，继续 No-Go。
- 2026-08-04 同一保留安装继续暴露 `S3 High`：真实 Provider 凭据为 `enc:` 持久化且最近 health 成功，但首任务无法创建 Session；原生窗口复核再次看到 `WorkflowLedgerMigrationError: Committed migration target durable history regressed`。根因不是 Base URL/Key，而是旧 committed v8 journal 没有 Conversation Ledger 证据，v9 readiness 又在新增表缺失时丢弃了其余已通过的 verification，导致 continuity 把 additive migration gap 误判为数据回退。源码已改为保留 partial verification 并新增旧 v8 -> v9 回归；`test:workflow-ledger-migration`、`typecheck`、production build、40/40 package audit、隔离 renderer 首启/重启和 `git diff --check` 通过，修复已绑定当前 D0 SHA。脱敏真实数据副本诊断进一步只复制数据库和迁移证据，在临时副本内重绑定绝对 journal 路径后证明 v8 -> v9、Conversation Ledger 建表、既有受检表行数保持、两个失败 IPC 恢复、可见错误消失和原件聚合摘要未变；Provider/URL/项目/会话文件未复制，临时副本已清理。当前运行安装仍为旧二进制，S3 必须安装当前 SHA 并复跑首任务后才能关闭。
- 2026-08-04 安装目录 High 的证据缺口已在 FIX-000 修复：旧流程只预检计划目录，Owner 随后另行启动安装器，二者没有共享 `/D` 或安装后 binding 证明，旧现场因此不能被诚实归因。新增 `RUN-FIX-000-ASSISTED-INSTALL.cmd` 在同一入口完成 clean-host preflight、以唯一末尾 `/D=<planned>` 启动交互 NSIS，并验证 EXE/卸载器及两条卸载注册命令均绑定该目录；成功后保留安装，不自动启动/卸载/清理。合同 smoke 通过，当前已有注册项主机的负向实测正确在安装器调用前停止；runner 已进入当前 SHA 绑定 Owner Kit，High 与 Owner gate 仍需 clean-host 正向复测。
- Release Doctor 现在输出机器可读的 `distributionPolicy`：正式跨平台状态继续由原 Doctor 与 12 资产矩阵决定；平台限定通道另有 candidate/final-notes/post-publication 三段门禁。`test:release-notes-audit:platform-scoped-final` 只接受 10 个精确资产、两架构正式 macOS 证据和 Windows unsigned-preview 原生证据，同时要求正式跨平台状态仍明确为 blocked；原 `test:release-notes-audit:final` 未放宽。
- `.github/workflows/release-candidate-evidence.yml` 保留为正式跨平台、只读、手动、不可发布的候选证据管线：Windows 正式证书采购前缺少签名 secret 会按设计失败，不能用 preview 配置绕过。该 workflow 的 blocked 状态与本轮 dirty-worktree“macOS 正式 + Windows unsigned preview”分发策略增量并存；后者不是最终发布决策。
- M1-T4 当时的公开进度快照为 `64 = 21 已验证 + 17 部分完成 + 25 立项目标 + 1 仅达到基础`；NFR-PRIV-001/002、ROUTE-006、ART-003、TRUST-002/003/004 以及 RUN-005/NFR-REC-003 foundation 推进后，当前仓库事实口径为 `21 + 26 + 16 + 1`。官网首页仍只保留当前能力、真实截图、上手/下载和精简状态入口，Assistant/Studio、24 项终极能力、10 阶段目标工作流、完整状态边界与 3D 愿景位于独立 `/vision/`、`/en/vision/`；该状态迁移不改变 21/64 已验证、43 项开放或 Release Doctor 的 `not_ready`，也不替代签名、公证或目标平台证据。
- 正式运行时现有默认 OpenAI-compatible API、可选 Claude Agent SDK，以及已注册到 Provider/SessionManager/UI 的原生 Anthropic Messages 三条执行路径。上述最新 Deep 的 3 个 optional skip 分别保留真实 Claude、China real-network、China tool-call parity 的外部条件，不计入 pass，也不阻塞默认发布档位；clean Deep 仍不能替代签名、公证、目标平台安装和最终资产绑定。
- Agent 恢复内核已升级为稳定事件身份 + 恢复游标 + 持久 Effect Ledger。`write_file`、`search_replace`、OpenAI/Claude 文件编辑、Git commit/merge/push、Renderer 文件与 Git Index 操作、文本 hunk 丢弃、managed-worktree create/remove/patch 和 GitHub/GitLab PR/MR 已接入专用只读对账；Operation Gateway、lease/fencing、强杀恢复、DAG autoMerge durable finalizer/receipt 已由 required E2E 覆盖。该边界不等于外部系统事务级 exactly-once，writer 仍可能在强杀/断电/ENOSPC 时留下半写文件。
- 2026-07-30 当前开发批次已在源码层补齐三类外部 Effect：`git_create_issue` 冻结 GitHub/GitLab 仓库与 marker，创建前查询并在重启后只读对账；带显式只读查询契约的 `mcp_call_tool` 可冻结 capability digest、查询参数与后置条件，普通 MCP 调用继续按 opaque 处理；`send_notification` 通过主进程加密连接器发送飞书/钉钉/企业微信消息，平台回执不明确、失败、超时或强杀时禁止自动重发。连接器属于全局用户配置，不随 Project 导出或删除；安全存储不可用时只在当前进程可用，持久化失败会回滚，损坏 Store fail-closed。当前 dirty-worktree 本地合成门禁已执行：`test-results/external-effect-recovery/2026-07-30T18-00-05-575Z/report.json` 以 `15/15` 覆盖 Issue/MCP 强杀后只读对账、零自动 Issue replay 与零自动 MCP replay；`test-results/notification-effect/2026-07-30T16-57-19-073Z/report.json` 以 `22/22` 覆盖三种消息通道、未知结果零自动重发与凭据零泄漏；完整 `npm run test:operation-effect`、typecheck 和 build 通过。真实外部平台、当前源码完整 Deep、所有入口运行时 enforcement 与 clean release 绑定仍未验证，因此只把 TRUST-003/004 推进为部分完成，不关闭 P0。
- Effect 入口清单 foundation 已进入 required gate 和 Deep 命令表：TypeScript AST 扫描当前发现 341 个工具、IPC、IPC action 与显式外部入口，登记为 118 个只读和 223 个 mutation；Effect policy 分布为 124 个 `none`、47 个 `opaque/manual_only`、143 个 `durable_local/idempotent_resume`、27 个 `queryable/reconcile_before_retry`，17 种 EffectTarget 均发现 Reconciler。最新 `test-results/effect-entry-inventory/2026-07-31T17-37-15-012Z/report.json` 以 `10/10` 通过并含 4 条负向路径；审查同时修正了会持久化状态的 `workflowLedger:reviewAcceptance` 被误标为只读的问题，并锁定其 durable mutation 合同。该静态门禁证明当前已发现入口都有显式分类、新入口/Target 漏登记会失败；它不证明所有潜在动态入口都已发现，也不证明 223 个 mutation 的权限、预算、强杀、对账和人工恢复均已逐一执行，因此 TRUST-002 仅为部分完成。
- `task-snapshots.db` 当前为 v9：保留 v6 TaskRun Effect evidence append-only hash-chain foundation、Goal/WorkItem/Run/Artifact/Acceptance/Evidence Link、全局 workflow event chain、canonical `workflow_recovery_sessions` 和持久 `workflow_store_identity`，并新增 `conversation_ledger_streams / generations / events`。JSONL 继续作为同步耐久源；DB archive 具有独立 hash chain，正常事件增量追加，Checkpoint 或链前缀变化创建新 generation 且永久保留旧代。启动会回填现有历史；恢复/分叉在 JSONL 缺失时从 DB 当前代重建，已有 JSONL 损坏时 fail-closed；Project 永久删除同步清理 archive，并将 stream/generation/event 移除量写入授权删除连续性。Task Snapshot/TaskRun 恢复读取仍支持 `legacy / compare / canonical` 三态，未显式配置时默认 `legacy`；旧库到 v9 继续使用备份、SHA-256、fsync、原子 rename、崩溃 checkpoint 和可回滚 journal。OpenAI Responses 服务端上下文仍只在 Provider/模型/协议/Key 身份匹配时复用；显式跨 Provider/模型分叉保留源 Project/Goal/WorkItem 与本地语义账本，但生成新 Session/SDK 身份且不继承旧 Responses/Claude SDK/Key 身份。OpenAI Responses/Chat、Anthropic Messages 与 Claude Agent SDK 均已接入 Provider-neutral 回放。2026-07-30 的 dirty-worktree required gate `npm run test:provider-neutral-recovery:required` 已验证首次归档/增量追加/Checkpoint generation rewrite、旧代保留、JSONL 缺失恢复、JSONL 与 DB 篡改 fail-closed、附件引用、tool-call/result 配对、禁止副作用重放、两个独立 Node 进程间 OpenAI 来源到 Anthropic 目标的 Project/Goal/WorkItem/Run/request/step 连续性、新 Session/SDK/Provider 身份和 predecessor/successor Attempt 链，并复用既有 Effect 强杀/close-race E2E。`sessions:restoreCheckpoint` 现统一经 SessionManager 阻断 `prepared / executing / waiting_reconciliation` 未决 Effect；dry-run 只返回带阻断原因的只读预览。新增三项 Deep required 子集为 `3 pass / 0 fail`，Node/Web 类型检查和 build 通过。本轮未运行当前 HEAD 的完整 Deep，也没有真实 Provider、完整 recovery ladder、clean commit 或发布资产绑定，因此 21/64 已验证、43 项开放与 Release Doctor `not_ready` 均不变；这不代表所有业务入口已经 canonical，也不等于完整 1.0 Workflow OS。完整 Artifact/blob/sourceRef 生命周期、统一 retention/export/import 和生产补偿计划/审批/执行仍未闭环。
- 2026-08-01 合并 `origin/main@79e54ae5` 后的 dirty-worktree recovery 定向回归已通过：`test:dag-finalization` 覆盖 summary receipt crash、零重复发送、verification unknown outcome、人工收敛、重试授权、Effect resolution、legacy migration 与 corrupt migration fail-closed；receipt 对账现在从严格耐久 transcript 重算并验证 `conversationLedger` 投影，summary crash 恢复后 transcript 与任务快照完整性一致。`test:conversation-ledger` 的 12 项 invariant、transcript restore、完整 `test:provider-neutral-recovery:required` 和更新到 v9 event identity 的 `test:dag-recovery` 同步通过；Node/Web typecheck、production build 与全树 `git diff --check` 通过。非 required 1.0 acceptance map 为 `102/102` structural pass，但 closure 仍失败：P0 `21/64 verified`、`43 open`、`116` 项 closure failure、critical recovery `1/11`，且 release binding 明确因 dirty worktree 失败。本轮没有运行完整 Deep，也没有 clean candidate、真实 Provider 或发布资产绑定，Release Doctor 继续 `not_ready`。
- 2026-07-31 的 Office 交付 foundation 已把 Word、Excel、PowerPoint、PDF 四种 production tool 接入同一 `prepare Effect → execute → reconcile → Artifact → Evidence → Acceptance → stage handoff` 链；来源文件在批准时冻结路径、设备/inode、字节数和 SHA-256，结构化输入、输出竞态、来源漂移、跨 Project 与伪造 confirmed Effect 均 fail-closed。批准的 Effect target 还冻结由 `title + specDigest` 对应确定性输出生成的 SHA-256 与长度；执行和恢复对账都要求真实字节精确匹配，同格式但内容不同的有效文件保持 unresolved。旧 confirmed Effect 缺少 v1 output binding 时可读但被 producer 隔离且不创建 Artifact；旧 waiting Effect 禁止人工确认已应用，只能确认未应用后 `abandoned` 并显式重新生成。handoff 同时要求 Artifact 至少有一个 Acceptance link、所有被引用 Acceptance 都存在且为 `passed/waived`，并拒绝缺少 v1 output-binding metadata 的旧 Office Artifact；Artifact 已落盘但 Acceptance 尚未提交的窗口在当前进程和 fresh-process readback 中都不会外泄。最新 dirty-worktree gate `npm run test:office-delivery:required`（`test-results/office-delivery/2026-07-30T17-50-16-033Z/report.json`）以 `40/40` 覆盖 4 个通过成品、1 个失败成品、6 个 confirmed Effect（5 个 v1 output-bound + 1 个旧版隔离）、7 个 canonical Artifact、5 passed + 1 failed Acceptance，其中仅 4 个 passed Acceptance 具备 handoff 资格，并覆盖 25 条负向路径、三时区确定性和独立 Node 进程重启幂等；`test:artifact-lifecycle:required` 最新 15 项与 Artifact Graph、typecheck、build、plan contract 也通过。当前确定性证据只证明同一生成器输入在三时区生成相同字节，不代表任意语义等价 OOXML ZIP 已完成规范化。当前证据仍绑定 dirty worktree、本地合成输入和结构/渲染检查；没有运行当前源码完整 Deep，也不等于真实 Provider、Word/Excel/PowerPoint 原生打开、真人 30 分钟交付、完整导出/返工、所有 Artifact producer/Acceptance ingress、完整强杀 checkpoint 矩阵或 clean release 绑定。因此 ART-001/003/005 与 AC-15 仍只是 foundation，21/64 已验证、43 项开放和 Release Doctor `not_ready` 均不变；strict closure failure 数量以本轮重跑的机器验收映射为准。
- 2026-08-01 的 verified-delivery 本地系统门禁已把 `research → requirements → design → implementation → review → repair → test → delivery` 串成同一 production Store/API 主链。最新 dirty-worktree 报告 `test-results/verified-delivery-flow/2026-07-31T17-49-16-131Z/report.json` 以 `24/24` 通过：8 个版本化 Artifact、9 条 Evidence、8 个 Acceptance、8 个 canonical WorkItem 与 8 个 Run 保持 Project/Goal/WorkItem ownership；Artifact/Evidence/Acceptance/Link/WorkItem attachment 各自在 production API 返回后实际收到 `SIGKILL`，随后由全新进程读回。pending/failed Acceptance 均阻断下游，passed/waived 才携带 Artifact ID、digest、location 进入下一阶段且无需用户重述。review 的原始 Run 以 failed 和错误 canary 保留，repair 动作生成同 lineage v2 并保留 supersedes history；故意篡改真实 Artifact bytes 时 Acceptance fail-closed，恢复字节后 retest 通过，最终状态保留 `7 completed + 1 failed` 的真实 Run 历史。两次 fresh-process ownership、Evidence-chain 和 production Studio Result digest 完全一致；Studio Result 导出包含 `errorDigest` 但排除原始 Run 错误 canary，并经测试报告确认脱敏。该门禁仍使用本地合成阶段内容，未证明断电/文件系统故障、真实 Provider、真人代码/Office 流程、原生 Office 打开、repair UI、远程/非文件 Artifact、完整导出下载或 clean release 绑定；ART-003/004/005 因此仍为部分完成，21/64、43 项开放与 Release Doctor `not_ready` 不变。
- Canonical ModelAttempt v1 已把 OpenAI-compatible 请求、模型 DAG、Claude Agent SDK turn 与原生 Anthropic Messages 每次底层 HTTP 请求接入 Run/WorkItem 归属、逻辑 request/step、Provider/model/protocol、route reason、usage 和不可变事件链；`started` Attempt 在强杀重启后会把 legacy TaskRun、canonical Run 和匹配 snapshot 原子投影为 `waiting_reconciliation`，普通发送/恢复/删除均 fail-closed。用户只能显式 `retry_authorized` 或 `cancelled_by_user`；授权本身不调用 Provider，successor 必须消费同一 requestId/stepId 并链接 predecessor，二次强杀不会复用旧授权。原生 Anthropic Messages 已注册独立 EngineKind 并接入 Provider/SessionManager/UI；saved Provider/Broker 目标绑定、`/v1/messages`、thinking/redacted thinking/text SSE、usage、HTTP/流错误、取消、`tool_use/tool_result` 多轮 NativeToolRuntime、权限/审计/Effect/幂等、40 请求上限、同 Provider Key 与仅限 Anthropic Engine 的 Provider failover，以及内容寻址图片重启恢复，分别由 `test:anthropic-messages:required` 17/17、`test:anthropic-tool-use-loop:required` 10/10、`test:anthropic-failover:required` 8/8、`test:anthropic-engine-registration:required` 和 `test:anthropic-image-restart:required` 覆盖。partial 输出、abort、账本失败或未决 Effect 会保守阻止重放；当前仍缺真实 Provider、clean release 绑定、完整恢复阶梯与统一 Run/Context 契约，因此 ROUTE-004/RUN-003 仍仅为部分完成。
- 2026-07-20 的 1.0 domain 增量已落地无目录 `ProjectWorkspace`、Goal Contract、WorkItem、Resource、归档/恢复/删除/manifest 导出，以及原生 RoleTemplate、DigitalWorker、Assignment、退休历史、lease/fencing 和 `done/completed` Evidence/waiver Acceptance Guard 基础；ProjectWorkspace、DigitalWorker 和 Acceptance targeted required smoke 均通过。Studio 已提供 Project/Goal/WorkItem 与数字员工基础 UI；固定 Assistant/Studio 控件在真实 Electron 中以鼠标、Space/Enter、唯一 `aria-pressed`、草稿/会话/转录不变、Office/搜索往返、遮罩层和 `1320x860 / 760x700 / 360x520` 六张截图完成 `9/9`。新增 running live-switch E2E 以单一 Responses 请求验证十次 Assistant/Studio 往返、流顺序/唯一性、重复发送防绕过、运行中模型切换 fail-closed、可见 UI 拒绝和 source/build 新鲜度。Goal/WorkItem 的生产 `list/get` 已默认切到 hash-chain verified rich view；生产命令现于持有 JSON CAS 锁期间先提交 Workflow Ledger，再把 JSON 作为 Workspace 可见性目录和恢复投影落盘。新 required smoke 已证明迁移时 JSON 仍是旧态、三个强杀检查点、死进程锁即时回收、重启 CAS 修复和无重复 replay；migration/digest/entity/compare 异常继续 fail-closed，只有显式 `legacy` 读模式才回滚读取。DigitalWorker 招聘已创建 CaoGen 原生岗位实例，v2 策略 schema 显式迁移并重启恢复；Assignment 加载、策略更新、lease 和 reassign preflight 会复核数据范围，拒绝发生在 owner 写入前；终态 Acceptance 还会执行 Evidence 下限和显式用户批准。新增真实 Electron required E2E 已以 `11/11` 跨三次启动验证 RoleTemplate 招聘、完整策略录入、WorkItem Assignment、重启无重复、UI 退休、拒绝新 Assignment、历史保留和外部 Agent CLI sentinel 零调用。tool/budget/concurrency/escalation 全动作执行、其他业务入口 canonical 化、Run/Artifact linkage、TEAM-005 完整保留/导出/删除策略和 clean release evidence 仍开放。
- 2026-07-29 的 Project 数据生命周期增量已输出 sealed/sanitized、owner-scoped、18 类对象的 Project Aggregate canonical JSON，并新增不要求先创建空 Project 的单文件导入。导入会校验 export/aggregate/ownership/credential 边界，保存 `0600` 私密源副本，通过 durable phase journal 合并 Workspace、DigitalWorker、完整 TaskRun/Artifact Graph/Evidence/Acceptance、canonical Learning，自动安装缺失且内容匹配的 RoleTemplate 依赖，冲突时不覆盖；目标 Workflow DB 先做 disposable-copy dry-run，写入后重建目标端 hash-chain 坐标，启动时续做未完成操作，最终重新封存并验证语义等价。删除后恢复会只替换同 Project 的 `workspace.purged` 墓碑，保留无关 Project。Studio 通过隐藏 JSON 选择器提供“一步导入”，重复或篡改文件 fail-closed。永久删除仍先写 `0600` 私密脱敏备份，再分阶段清理当前参与者集合、写授权删除账本和 deletion proof，并保留外部 Resource。专项导入 gate 与 Node/Web 类型检查通过；真实 Electron 生命周期报告 `test-results/project-workspace-lifecycle-ui/2026-07-29T16-07-10-846Z/report.json` 以六次启动 `19/19` 覆盖导出、永久删除、单文件恢复、无关 Project 保留、Run 读回、重复/篡改拒绝和重启持久化。全 29 Store owner proof、统一 retention、Artifact blob 打包、Session/transcript/snapshot/ModelAttempt 纳入导入、旧路径 Memory 映射、其他 connector 合同和全 inventory 删除证明仍开放；消息连接器已明确为 Project 边界外的全局用户配置。因此 `PROJ-004`、`NFR-PRIV-001`、`NFR-AUD-004` 保持部分完成，21/64 已验证、43 项开放与 Release Doctor `not_ready` 均不变；dirty worktree 自动化不是 release-ready 证据。
- Workflow Acceptance repair/retest 与结构化失败接入本地基础已接通：failed review 按 Acceptance ID + revision 确定性创建同 Project/Goal/parent/owner 的 canonical repair WorkItem 及其 Acceptance；并发/重复创建幂等，绑定冲突 fail-closed，启动时恢复已提交失败但缺失的 repair。repair 未 `done` 且 Acceptance 未 `passed/waived` 前拒绝 retest；完成后原 Acceptance 清空本轮 Evidence/Verifier 并进入新的 `verifying` revision。多 criterion Acceptance 已要求每项绑定非空 Evidence 和 criterion-scoped `verifies` link；可选且不可变的 `criterionPolicies` 一旦声明就必须全量覆盖 criteria、固定 criterion ID/index，并把每项限制为指定 Workflow Evidence kind 与允许 source，Task Effect origin 或 kind/source 不匹配均 fail-closed；无 policy 的旧记录继续兼容。review/retest 保留原 policy；repair-derived Acceptance 在新建、重复恢复和启动恢复时继承同一 kind/source 约束，并将 criterion ID 重新绑定到 repair WorkItem 的确定性 criterion。对带 policy 的 Acceptance，typed cross-validation/test failure 在未显式提供 `criterionIndexes` 时必须恰好匹配一个语义兼容的 review/test criterion 才能自动绑定，零个或多个匹配均 fail-closed 且不落 Evidence。终态 canonical gate 会重新解析 live store：Workflow Evidence 必须匹配 `workflow.evidence.recorded` envelope/payload digest，Task Evidence 必须匹配 `workflow.effect.evidence` 事件及 Run/Effect source；Acceptance passed 后删除 Workflow Evidence、Task Evidence 或 Evidence Link 会在 ProjectWorkspace 源提交前 fail-closed。passed Workflow Evidence 绑定 Artifact 时，门禁还要求 Artifact digest、Evidence content digest、available 本地 path 或 `file:` URI、声明 checksum/size 与稳定读取的真实常规文件字节一致；缺失、删除、篡改、符号链接、remote-only 或任一 available 本地副本异常都会在 ProjectWorkspace 源提交前 fail-closed。受信 main-only ingress 会把 typed cross-validation/test failure 原子写成 immutable Evidence、criterion links、failed Acceptance revision 与 audit event，并恢复缺失 repair；交叉验证生产者只接受首行结构化结论，只有 `BOTH_NEED_FIX` 或在 reviewer 已给出 `CONCERNS/BLOCKED` 时的 `REVIEWER_OK` 才接入，`PASS`、畸形文本、`PRIMARY_OK`、`NEED_HUMAN` 均不接入。原生 `bash` 工具结果现携带结构化 `commandTermination` 与 `exitCode`；只有绑定当前 Session/TaskRun/ToolExecution/canonical testing WorkItem、事件与输入/输出摘要一致、显式测试命令，并同时满足 `commandTermination === 'exited'`、`isError === true`、`exitCode` 为非零安全整数的真实 `tool-result`，才会生成只含摘要与 SHA-256 的测试失败 Evidence 并进入同一 repair 路径。`timed_out`、`aborted`、`output_limit`、`spawn_error`、`not_started` 等基础设施终止不会误报 Acceptance failure；普通 turn 错误、非测试命令、缺失退出码或跨项目 Run 同样拒绝/忽略。Snapshot barrier 固定为 `capture -> flush -> persist -> delete`，flush 受 per-session failure latch 约束；启动恢复会补齐快照已提交但 ingress 或 Run 绑定未提交的状态，并在 replay conflict 时 fail-closed。ART-002 的 policy authoring UI 与 Acceptance review/evidence UI 已由真实 Electron required gate 覆盖创建、多 criterion kind/source、空 source 拒绝、按 criterion 选择匹配 Evidence、通过和重启一致性；WorkItem transition/lease 控件也由真实 Electron required gate 覆盖状态图、owner-bound lease、终态清理和重启持久化。Supervisor pause/cancel/resume/retry/reassign 已接入受信 main-process SessionManager 控制切片；该能力仍不等于完整 WORK-004/ART-004：其他工具/引擎测试生产者、自动测试编排、Studio 控制 UI、自动 repair Run、独立 Verification/不可变端到端链、repair/retest review UI、跨域 strong-kill 和 clean release-bound evidence 仍开放。
- Supervisor 的持久状态/IPC foundation 已有历史 `test:supervisor-state:required` 证据；2026-07-31 当前源码又分别通过 core、restart、TaskRun bridge 与受控 SessionManager focused gates（报告为 `test-results/supervisor-state-smoke/2026-07-30T17-05-16-491Z/report.json`、`test-results/supervisor-restart-e2e/2026-07-30T17-08-47-161Z/report.json`、`test-results/supervisor-taskrun-bridge-smoke/2026-07-30T17-08-47-091Z/report.json`、`test-results/supervisor-session-control-smoke/2026-07-30T17-05-54-480Z/report.json`）。当前边界除 pause→resume→reassign→cancel、failed→retry→resume、强 revision/lease/fencing、retry 快照预检、发送门禁和重启 hydration 外，还把 Goal 的 maxRuns/maxConcurrentRuns/maxTokens/USD cost 聚合约束接入 canonical send reservation/turn/retry；`authorizeTurn` 只允许 `queued/running`，paused、blocked、waiting_approval、waiting_reconciliation 均拒绝且不写 revision/event。renderer 直写 lease/start/block/reconcile/fail/complete/approval 只允许 manual Run；`task_run` 的 pause/resume/cancel/retry/reassign 必须命中 SessionManager runtime，否则 fail-closed，renderer 过期租约恢复也只处理 manual Run。新增 Electron IPC 负向用例已写入源码，但本轮沙箱禁止监听 `9970-10069`，提权审批服务又返回 `503`，所以当前构建上的 live IPC 新路径尚未执行，不能把 focused pass 写成完整 `test:supervisor-state:required`。pause/cancel/reassign 的 store 与执行器动作仍不是跨文件事务；Studio UI、所有执行入口的预算/并发一致性、自动编排和真实 Provider parity 仍开放。因此 `RUN-004`、`WORK-004` 与 `NFR-REC-004` 仅为部分完成，不构成 release evidence。
- 首个 canonical 跨域强杀恢复门禁 `npm run test:domain-restart-parity:required` 已纳入 Deep；最新 dirty-worktree 报告 `test-results/domain-restart-parity/2026-07-31T02-56-08-707Z/report.json` 绑定 `b37c17cf8dea`、worktree count `405`，以 `9/9` 覆盖 Project→Goal→WorkItem→TaskRun→Supervisor→opaque Effect。Effect 已进入 `executing` 且外部执行计数为 1 后实际收到 `SIGKILL`；全新 Node 进程将 Effect/TaskRun 恢复为 `waiting_reconciliation`，拒绝自动续跑，过期 lease/旧 fence 被拒绝，canonical ID、ownership、revision、`runRefs` 保持一致，二次恢复完全幂等且自动 replay 为 0。因此 `RUN-005` 与 `NFR-REC-003` 推进为部分完成；所有非终态、Board、Approval、Artifact、Acceptance、真实 Provider、当前 Electron UI、跨文件补偿和 clean release 绑定仍开放，报告自身也明确 `closesRun005=false`。
- 统一 Learning 生命周期已覆盖自动/模型 Memory、自动 Skill review 与 `optimize_skill`：三类入口只创建 project-scoped draft，保留来源、置信度、payload digest、目标路径与完整 before/after diff；仅主进程可信用户决定可 approve/reject/revoke/rollback/delete。单调版本、expiry、重启审计、项目隔离、symlink-safe journaled Skill 物化/对账，以及仅把 approved/unexpired Memory 注入 Anthropic、OpenAI Chat/Responses prompt 均由 required gate 和完整 Deep 覆盖。该边界不等于 TEAM-007 Worker memory namespace、退休 Worker 行为、全项目 retention/export/privacy 或 clean release-bound evidence。
- 1.0 验收映射已成为机器可读门禁：`npm run test:1.0-acceptance-map` 当前对 64 个 P0 和 38 个 P1 达到 `102/102` 唯一映射并通过结构自测，Release Doctor 已消费该结果；最新 dirty-worktree 报告（`test-results/product-1.0-acceptance-map/2026-08-01T02-01-48-074Z/report.json`，绑定 merge commit `8ba60148`）显示结构通过但严格 closure 仍失败：21/64 个 P0 当前已验证、43 个仍开放，120 个声明 gate command 中 100 个已实现、80 个 requirement 具备 implemented gate、116 项 closure failure，且 release binding 明确失败于工作树不干净。EXP-002 已由真实 Electron 的 5/5 检查证明 Assistant/Studio 共用 canonical Project/Goal/WorkItem/Run/Artifact；PROJ-003 以 27 项检查和 `notProved=[]` 关闭 Project ownership；NFR-PRIV-004 以 13 项本地 Provider parity、7/7 真实 Electron 零选择门禁、当前 Provider Profile dirty-worktree Service/Store `135/135`、真实 `SIGKILL` restart `13/13` 和真实 Electron `54/54` 门禁通过本地安全切片。`NFR-AUD-001/002` 现为 canonical audit timeline 部分完成；ART-001、RUN-002、TEAM-003 仍仅部分完成。版本号、Deep 全绿或结构映射通过均不能替代 1.0 product closure。
- ✅ **32 并发压测:修复后 7/7 error=0**(连跑 3 次稳定)。根因=瞬时并发打爆 socket 层;修:并发闸门(默认 8 在途)+ 瞬时网络重试。压力脚本口径已修(idle/error 分统计、error=0 独立断言)
- **Claude 登录不是必需项**。Claude 不再是默认引擎；真实 Claude E2E 默认 optional skip，只有显式设置 `CAOGEN_CLAUDE_REAL_E2E=1` 才会从 `~/.caogen-private/provider-parity.json` 选择 Anthropic 配置并产生真实调用，不再读取普通环境变量中的模型 API Key。应用内用户显式选择 Claude 专项时仍按 Provider Broker 或有效 Claude 登录态运行。
- 2026-07-29 的 W1 targeted 增量已实现会话级“查看/规划/执行”合同：规划可拆解但不能派发；结构化计划记录 DAG 依赖、预计产物、数据外发、成本、风险和 Acceptance，并以不可变顺序版本、SHA-256 摘要、变更原因及 append-only 批准/撤销/superseded 事件保存到本地私有 `userData/task-plans`。批准精确绑定当前 `version + digest`，新版本自动废止旧批准；send、子 Agent、DAG 首层/后续层和手工终端/Git/文件/worktree 写入口均在主进程复核，`bypassPermissions` 不能绕过。已绑定 Project 的批准现会把稳定 step ID 幂等投影为现有父 WorkItem 下的 canonical 子 WorkItem，依赖、预期产物 AcceptanceSpec 和投影回执均可重启复核；新版本可更新/新增未启动步骤、取消已移除且未启动的步骤，运行中或终态冲突 fail-closed；未绑定 Project 的对话只保存计划，不创建隐藏 Project。`npm run test:task-plan-contract:required` 已覆盖 canonical 投影/幂等/重启/增改删/冲突/对话边界；typecheck、build 与真实 Electron targeted E2E 已通过，最新报告为 `test-results/assistant-studio-ui/2026-07-29T06-04-08-795Z/report.json`（`15/15`、9 张截图）。这些是 dirty-worktree 功能证据，不是 clean release 绑定；计划合同自身的 Ledger 历史、多审批主体、全连接器执行门和 W1 真人主链仍开放，因此 `WORK-005/006` 未关闭。
- 同日追加自然语言 Goal Task 零配置入口：Studio 在 active Project 中只需输入一句“想完成什么”，主进程用稳定 `requestId` 幂等创建 canonical Goal 与父 WorkItem，随后按目标自动路由 Provider、建立 `workspaceId/goalId/workItemId` 精确绑定的 Session 并发送首条目标。无目录 Project 会优先使用 directory/repository Resource，否则使用 `userData/workspace-execution/<workspace-hash>` 应用隔离目录；不会降级成“对话”，不会创建隐藏旧目录型 Project。侧栏现同时读取 canonical Workspace 与兼容旧目录 Project，`workspaceId` 所属的活动 Session/历史记录优先归入 canonical 项目，只有没有 canonical/legacy 归属的 Session 才进入“对话”；项目行提供展开/收起、更多和 `+`，其中 canonical `+` 直达该项目的一句话任务入口，顶层 `+` 直达无目录新建项目。Goal 已写而 WorkItem/Session 失败时保留可见记录并以同一请求恢复；归属、目标或终态冲突 fail-closed，渲染层同步锁阻止双击重复 Session。最新合成 Provider Electron gate `16/16` 跨 5 次启动通过，含桌面/`390x844` 侧栏归组、控件、重启历史归属和移动端零溢出共 11 张截图：`test-results/project-workspace-lifecycle-ui/2026-07-29T07-20-26-251Z/report.json`。typecheck、build、ProjectWorkspace、canonical write-source、Project ownership、session ownership、plan projection、私有 Provider 配置脱敏和 worktree secret scan 均通过；编码规范审计仍为 `16 pass / 6 warn / 29 fail`，本轮 Sidebar/store 已低于旧硬上限，相关两项仅因指标改善要求刷新基线，其余为既有 dirty-worktree 问题。证据仍绑定 dirty worktree，不是 clean release 或真人首任务证明。
- 同日完成 NFR-PRIV-001 本地数据生命周期 foundation：`src/main/data-lifecycle/local-data-map.ts` 当前机器登记 32 个数据条目、53 条路径和 17 类顶层 Project aggregate 对象；完整 Project Aggregate export 另按精细类型覆盖 18 类（包含 Artifact Edge、Artifact Location、Evidence Link）。最新 required report `test-results/local-data-map/2026-07-31T08-31-42-062Z/report.json` 通过 `716/716` 项检查且 `unregisteredSources=[]`，但 29 个条目仍为 `partial`、3 个仍为 `inventory_only`，没有 `enforced` 条目。owner-scoped 完整脱敏导出、当前参与者集合的 durable import/readback、首批 crash-recoverable Project cascade、备份读回和 deletion proof 已完成；消息连接器作为全局用户配置排除于 Project export/delete。统一 retention、未纳入 coordinator 的 Store owner proof、Artifact blob/Session/ModelAttempt/旧路径 Memory、其他 connector 合同和全 inventory 删除证明仍开放，因此 NFR-PRIV-001 保持部分完成；证据绑定 dirty worktree，不是 release-ready。
- 2026-07-31 的出站上下文策略 foundation 已为 Project Resource 增加 `S0-S4` 数据等级与 `allow / local_only / deny` 外发策略；`S3` 强制归一为 `deny`，拒绝资源在构造 Provider prompt 前排除，`local_only` 只允许 loopback Provider 并冻结原 Provider ID，使同 Provider 换 Key/模型可继续、跨 Provider failover（包括另一个 loopback）在 Attempt 前拒绝。OpenAI-compatible 每次 retry、原生 Anthropic 每次 HTTP/tool loop/failover、CaoGen 可见的 Claude queued turn/重发/failover，以及 SessionManager 的 DAG decomposer 直连请求均在 Attempt/network 前重检 content-free manifest、manifest digest 和 Project revision/policy digest；DAG 另绑定实际 request-body digest。Claude `@文件` 已改用 canonical `realpath + relative` containment，并拒绝越出 cwd 的同前缀兄弟路径与 symlink。Studio 可配置资源策略，Composer 显示接收方、模型、本机/远程、数据等级、排除项和 `部分范围`。dirty-worktree 报告 `test-results/outbound-context-policy/2026-07-31T08-31-41-388Z/report.json` 与 `test-results/routing-hard-policy/2026-07-31T08-31-41-697Z/report.json` 均绑定 `b37c17cf8dea`、worktree count `421`，各通过 `21/21`；Node/Web typecheck 与既有 ModelAttempt required 19 项同步通过。该证据只覆盖消息/图片、自动 Project Resource、DAG request body 和已显式投影的部分 Memory/IDE/workflow/conversation 标签；engine-owned system prompt、Skill/tool schema/result、全部历史、MCP/connector/其他自动入口、统一 request-body/Run/WorkItem/approval 持久绑定、Claude SDK 内部后续模型请求，以及 region/domain/capability/permission/budget 的统一 Router hard filter 均未闭环，Composer 在预览尚未生成或失败时也未强制等待。因此 `NFR-PRIV-002` 与 `ROUTE-006` 仅推进为部分完成，不能据此声称完整出站预览、完整 no-egress 或 release ready。
- 同日完成 WorkItem 本地单用户转交 foundation：Studio 可将进行中的 WorkItem 转交给同 Project 的人员或 active DigitalWorker，并强制填写原因；主进程注入可信本地用户 actor，Renderer 不能自报身份。转交在同一协调链中更新 WorkItem owner 与 active Assignment，保留旧 Assignment 历史，撤销旧 WorkItem lease，立即重算旧/新 owner 权限，并记录 journal/audit；CAS、同 requestId 幂等、重启读回，以及跨 Project、停用 Worker、active DigitalWorker lease 冲突和策略拒绝的零副作用边界均由 `npm run test:work-item-transfer:required` 覆盖。真实 Electron gate `npm run test:work-item-transfer:e2e` 覆盖 Studio 转交、表单在移动端不产生横向溢出、提交后 owner/Assignment 可见和重启读回。该能力只证明 `COLLAB-001` 的本地单用户任务转交基础，不证明真实多用户身份、评论、提及、共享审批、组织策略或 Webhook；证据绑定 dirty worktree，不改变 P0 统计，也不是 release-ready。
- 同日完成统一结果工作台与 canonical 审计时间线 foundation：Studio 顶部新增“结果”，Assistant 的结果入口复用同一 renderer-safe 合同，Memory 保持独立工具。主进程只按当前 Session 的 canonical `workspaceId/goalId/workItemId` 聚合 Project、Goal、WorkItem、历史 Run、Artifact 位置/版本/摘要、Evidence、Acceptance、测试、成本覆盖率、风险、未完成项和审批；审计时间线从保留的 canonical source records 投影 Actor/岗位、Provider/model、Key 标签、路由原因、已知成本、Tool/result 摘要、Effect、Evidence、Approval 与 Acceptance。按 Run 筛选、opaque cursor 分页、源变化后的陈旧游标拒绝、WorkItem/Run 隔离、缺失引用完整性行和 typed integrity error 均 fail-closed；Renderer 不取得 prompt、原始工具输入输出、Effect target、原始失败文本或凭据。持久 ownership/aggregate 冲突仍 fail-closed；Session 激活在 canonical write 锁内校验和投影，live aggregate 对瞬时 revision 冲突执行有上限稳定重读。交付导出使用 canonical JSON 和 SHA-256 摘要；六个结果快捷动作继续复用变更、文件、预览、浏览器、终端和任务/DAG 面板。最新 `npm run test:audit-timeline:required` 已通过审计合同 `9/9`、结果合同 `28/28`、production build 与真实 Electron `6/6`；报告 `test-results/audit-timeline-required/2026-07-31T17-10-36-501Z/report.json` 和 `test-results/studio-result-surface/2026-07-31T17-10-54-389Z/report.json` 绑定 dirty merge commit `8ba60148`，含 14 张 `1320x860 / 760x700 / 360x520` 截图且页面/面板横向溢出为 `0px`。模型请求命中本机 mock，无真实 Provider 调用。该证据只把 `NFR-AUD-001/002` 推进为部分完成，不证明每个历史生产者/成本记录、真人 30 分钟代码或 Office 交付、clean release 绑定，也不关闭 M2-T8、W1、`EXP-005` 或 `ART-005`。
- 零配置本地算力增量已落地：Assistant 首屏只探测固定 loopback 地址上的 Ollama、LM Studio 与 vLLM，发现运行中的兼容服务后自动激活；本机无鉴权 Provider 不需要伪造 Key、不发送 `Authorization`，可以不创建 Project 直接发送。无鉴权模式只允许 loopback OpenAI-compatible Provider，远端目标 fail-closed，重复激活幂等；设置页提供“使用本机模型”恢复入口。当前 dirty worktree 的真实 Electron required gate `5/5` 通过，覆盖桌面/移动截图和 `360px` 零横向溢出（`test-results/local-compute-zero-config/2026-07-29T05-28-17-080Z/report.json`）；零选择路由 gate `7/7` 通过（`test-results/routing-zero-choice/2026-07-29T05-30-38-811Z/report.json`）。该证据不调用付费 Provider，也不证明通用零前置：没有已安装并运行的本地服务且没有用户 Key 时仍无可执行算力，因此 M2-T6 保持开放。
- M2-T7 开箱任务已完成自动化基础：Assistant 首屏提供“读懂项目 / 审查改动 / 整理文件成报告 / 规划复杂任务”四个跨代码与办公场景的预设任务，各自绑定 `view / execute / plan` 策略。为避免在没有真实目录时误发，“读懂项目”只填入原始 prompt 并聚焦输入框，用户确认目录后再提交；其余三个预设点击即创建“对话”并发送。报告预设要求保留源文件、引用来源并生成或增量更新 `CaoGen-report.md`；无可分析文件时要求恢复提示，不允许臆测。同步提交锁阻止双击创建重复 Session/请求。历史真实 Electron 报告 `5/5` 证明四预设清单/映射、报告策略与 Prompt、报告预设无 Project 双击单 Session/单请求，以及 `1320x860 / 360x520` 布局；它没有逐个点击执行另外三个预设。M2-T7 的实现切片已完成，但 M2-T1/T2 的真人首任务成功率和完整预设交互验证仍开放。
- D0 首任务引导的当前 P1 安全增量已修正运行候选被 Provider 水合覆盖、`error` 误报 `running` 和 await 后读取全局 `activeId` 的候选误绑定窗口；`createSession/startSessionWithPrompt` 现返回确定 Session ID，首条消息定向发送并用同一 ID 登记候选。Workbench 为当前失败候选提供显式重开入口，module 级提交锁阻止并发重复并于失败后释放；完成判定绑定候选 Session，旧 Result 快照请求在用户重开后不能污染新的空 onboarding 记录。最新 dirty-worktree Node 行为门禁 `test-results/first-task-onboarding/2026-07-31T08-31-41-212Z/report.json` 绑定 `b37c17cf8dea` 和 421 项 dirty worktree，以 `15/15` 覆盖并发第二次提交零 create/send、失败重试、running reload 零副作用、六态/四步投影、失败候选 compare-and-reset、跨 Session/陈旧 Result 隔离、完成谓词与隐私 canary；typecheck、build 和范围内 diff check 通过。该证据不是当前 Electron 鼠标/键盘/reload、真人首任务、真实 Provider 或 clean release 证据，M2-T2 保持开放。
- P1 全部可做项收口(2026-07-06):全文搜索、冲突三栏+合并回执、插件安装/卸载/版本/权限、CLI 真验
- Work OS 第一波已进入 main:A1 Drive、A2 Quickbar、A3 Desktop Control、A4 Code Forge、A5 Skill Fabric、A6 Memory Loop、A7 Control Center、A8 Personal OS、A9 Genesis(计划层)。Genesis 只宣称编排/交付计划,不宣称真实外部子 Agent 执行、自动合并、推送或发布。
- 2026-08-07 已退役 CaoGen 的 VS Code/JetBrains 专用插件与本机 WebSocket IDE Bridge；旧 P2-005 插件构建和 Host/recorder 报告只保留为历史证据，不再构成当前发布门禁。P2 release scope 现只要求 P2-002/P2-003；P2-001 GUI 与 P2-004 中国模型外部证据仍按声明范围独立核验。替代方向是 CaoGen 内建代码工作台，不通过外部 IDE 插件回补能力；当前仅完成退役与门禁切换，内建 LSP、诊断、导航、重构、测试和调试闭环仍开放。
- 2026-08-10 内建代码工作台已补齐受限测试命令发现与一等 UI 入口，支持运行/取消以及 `passed / failed / cancelled / timed_out / output_limit / launch_failed` 结构化结果；失败证据只保存工作区摘要、命令身份、输出摘要、行数和脱敏摘要。新增“调试”一级入口，绑定项目目标清单和项目相对断点，基于 Node Inspector 提供断点、继续、暂停、单步、停止、调用栈、局部变量和对象展开。调试专项 smoke `16/16`、可见 Electron UI `13/13`；测试工作台 smoke `26/26`、可见 Electron UI `11/11`、文件编辑器回归 `29/29`、TypeScript LSP 与 durable writer inventory 通过。`CODE-002` 技术关闭；`IDE-001/IDE-002` 仍因重构、大型仓库/崩溃恢复、键盘可达门禁和真人任务缺失而开放。
- 同轮 GUI Effect 恢复已通过真实 Windows Notepad：`test-results/gui-input-preflight/2026-08-07T13-49-16-282Z/report.json` 的精确 window-absent target 在动作后只读对账为 `confirmed`；GUI required 为 `49/49`。18 个新增权限/Provider IPC 已按只读、本地或 opaque 外部副作用登记，8 个授权/余额网络入口进入 Effect Ledger 且只持久化选择器摘要；inventory v10 两套门禁、Provider 授权 `118/118`、余额/高级配置 `22/22`、reconciliation、typecheck、production build 和串行 page smoke 均通过。视觉/模糊 GUI 条件仍只允许人工对账，任何 GUI mutation 均不自动重放；这些 dirty-worktree 证据不升级 `FIX-000 / No-Go`。
- 私有 Provider 配置已由 fresh required gate 实测：6 个真实目标、72/72 golden tool-call cases、`maxGap=0`，报告为 `test-results/china-tool-call-parity/2026-07-22T09-54-35-764Z/report.json`。门禁仅对 `429/5xx` 或网络异常做最多三次短退避，最终 4 个用例发生重试、72 个结果均为 HTTP 200；没有降低 parity 标准。该结果不替代 clean candidate 上包含 send/tool/artifact/recovery/usage/billing 的真实默认 Provider release record。
- 当前 Intel 主机存在 1 个有效 Developer ID Application 身份，0.1.7 release config、entitlements、Hardened Runtime 和签名配置预检通过。本地 x64 签名基线已验证 Developer ID、TeamIdentifier、45/45 Mach-O、DMG/ZIP 内签名和真实 renderer 启动；它显式跳过 notarize，且构建时尚未嵌入提交 provenance，因此不是最终候选。新版负向 audit 对 app、DMG 内 app、ZIP 内 app 同时拒绝缺失 provenance，并保留 Gatekeeper/stapled ticket 共 6 项失败；未来 release config 会把 schema、完整 Git SHA、clean 状态与版本写入包内 `package.json`，required audit 必须在三份 app 上一致验证。当前进程没有完整公证凭据，历史 `2026-07-22T09-50-24-743Z` API-key 认证结果不冒充当前配置。
- 本轮 `node scripts/secret-scan.mjs --worktree --history` 已通过，覆盖 tracked、staged、worktree 内容、敏感文件名和 Git 历史；该 dirty-worktree 结果仍不替代精确 clean release commit、干净工作树 Deep 和 packaging 发布门禁。
- 本地真实 Provider 测试只从 `~/.caogen-private/provider-parity.json` 读取私有配置，目录/文件分别强制 `0700/0600`，拒绝符号链接、非 JSON、内联 JSON、宽松权限、超限文件和生产模式自定义路径；旧 Chat/Coding/Cross-route/Orchestration/Stress E2E 已移除 `CHAT_E2E_KEY/BASE_URL/MODEL` 注入，并统一要求显式真实调用开关。普通 Deep 与常规回归继续使用合成凭据，不会自动产生真实调用或费用。`.caogen-private/` 已被 Git ignore，secret scan 对强制加入 Git 的同名路径 fail-closed 且只报告位置/规则，不回显凭据；preflight、doctor 与 parity 报告只保留匿名序号、协议聚合、计数和结果，不保存 Provider 标识、Key、Base URL、模型清单、响应原文或派生明文。该约定只描述本地测试边界，不允许把私有 JSON 内容写入源码、日志、报告和公开资产。
- GitHub Releases 公开资产审计继续要求名称、大小、状态、SHA256 与 `latest*.yml` 文本全部可读;若发布后的远端 read-text 审计超时或失败,只保留本地校验结论,不得宣称公开文本资产已完成扫描。
- 历史 v0.1.7 五资产发布说明保存在 `d8e883a2:docs/RELEASE-NOTES-FINAL.md`，列出精确 5 项资产及 SHA-256，并曾作为 GitHub Release 正文；当前工作树的同名路径已复用为 v0.1.8 历史候选说明，不能作为未版本化的 v0.1.7 合同来源。
- Packaging gate 的 v0.1.6 macOS x64 DMG/zip、两个 blockmap 与 `latest-mac.yml` 资产集 SHA256 为 `5ba568959b4973c7fa07a138ff80d1767be8945a9a76d155487b1d6556dc677b`。macOS 包仍未签名,Release notes 保留首次打开说明。
- v0.1.6 打包启动回归发现并修复 `tree-sitter` 运行时缺少 `node-gyp-build` 的主进程崩溃:`node-gyp-build` 已提升为应用直接依赖,`release-packaging-audit` 会解析 `app.asar` 并阻止缺失运行时文件的包通过,`test:packaged-app:mac` 会从全新用户目录启动成品并要求出现真实 `CaoGen` renderer。修复后的 macOS x64 `.app` 已通过该启动测试;仍未签名/公证。
- 项目级规则口径更新:`caogen.md/.caogen.md/README.md` 向 OpenAI-compatible 与原生 Anthropic Messages 两条运行时注入项目身份与规则。未配置规则的新项目也会注入项目身份和缺失规则提示;设置页项目规则已提供结构化编辑器,可同步编辑项目提示词、背景、技术栈、常用命令、测试/构建命令、禁止目录、隔离策略、模型调度策略、项目记忆与历史决策;`caogen.md` 的模型调度策略会进入智能路由理由。由 `node scripts/context-loader-smoke.mjs`、`npm run test:project-rules-ui`、`node scripts/model-router-smoke.mjs` 覆盖。
- 多厂商配置口径更新:Provider 已支持多 API Key、活动 key 选择、行内连通性检测、模型列表同步和持久化健康状态。Provider Credential Broker 使用当前活动 Key 向 OpenAI-compatible、原生 Anthropic、DAG 和模型发现 HTTP 请求注入受管凭据头；不再生成 Claude Code 子进程环境变量。活动 key 遇到鉴权、403、限流或余额/配额错误时,会先切到同 Provider 内的可用备用 key；备用 key 池耗尽后才进入同协议 Provider failover。当前只宣称 Provider Broker 基础,不宣称完整 project/session/operation/expiry 作用域、凭据迁移 crash fault injection、全出口 secret canary、主动额度探测或按 key 权重负载均衡已完成。
- 产品定位门禁新增: `npm run test:product-positioning:required` 会扫描 README、欢迎页入口文案、Release notes、Release gate 和公开品牌入口,防止公开文案重新出现固定未来版本目标、外部产品名称/对比话术、开发者-only 定位、未验证的中转站/Office 版式过度宣称,以及旧的菱形占位 logo。最新通过报告见 `test-results/product-positioning-audit/latest.json`;v0.1.5 release doctor 已在 release commit `d9969e3` 上绑定版本、当前 commit 与干净工作树并达到 `status: ready`。
- 自动调度口径更新:设置页已支持有序自定义规则、项目级模型偏好、Provider 健康过滤、预算限制、结构化决策日志和跨厂商备用目标。`npm run test:failover-target` 已验证 OpenAI-compatible 与原生 Anthropic 两条 failover 路径都会更新固定模型，UI 会显示可读的切换原因与重试目标。当前不宣称自然语言策略编排器、按 key 额度调度、跨月精确成本账本或长期趋势分析。
- 自动调度第五阶段最新闭环:均衡、成本优先、质量优先和速度优先已成为四个独立策略;速度优先先按延迟档排序,同档再参考历史延迟 EMA,专项测试证明它与质量优先会对同一复杂任务选择不同模型。策略优先级为“项目规则 > Core 用户策略 > 专用工作模式预设”;项目 `caogen.md` 的“速度优先”不会再折叠为均衡,Core 不再覆盖用户策略,专用模式仍保留自己的预设。设置页已真实保存 `schedulerStrategy: speed` 和自定义规则 `whenStrategy: speed`;聊天详情、控制中心和 3D 办公选中 Agent 面板均显示有效策略及延迟依据。当前完整深测口径为 `157 total / 155 required pass / 2 optional skip / 0 blocked / 0 fail`。当前仍不宣称自然语言策略编排器、按 key 额度调度、跨月精确成本账本或长期趋势分析。
- macOS 顶部菜单栏图标已与 Dock/应用图标分离:菜单栏使用 18×18 / 36×36 Retina 的透明单色 `trayTemplate` 轮廓并启用 Electron Template 模式,可随 macOS 深浅色菜单栏自动着色;Dock、窗口、应用内品牌与安装包继续使用正式全彩人物 Logo。`npm run test:macos-tray-icon` 已验证 PNG 尺寸/透明通道、打包资源声明、Electron `nativeImage` 加载、Template 标志和真实 Tray bounds(`test-results/macos-tray-icon/latest.json`)。
- 文件预览口径更新:HTML/Markdown/Text/CSV/JSON/图片/PDF 已有真实预览;PDF 已接入文本层 best-effort 提取并可发给 Agent;`.docx/.xlsx/.pptx` 已接入 OOXML 文本与结构提取。macOS 通过独立 Quick Look IPC 生成完整系统文档预览包,HTML/CSS/JS/图片附件全部内联,CSP 禁止网络,renderer iframe 仅开放 sandbox 脚本;完整预览失败时回退首屏 PNG,再失败则保留结构视图。结构视图已支持 Word 显式分页、Excel 工作表和 PowerPoint 幻灯片的上一项/下一项/选择器导航,可把当前页/表单独发给 Agent,批注会保存页码、摘录和结构选择器。`npm run test:office-visual-preview` 已用真实 DOCX 验证 625×980 系统文档预览、缓存、路径边界和附件/外链封锁(`test-results/office-visual-preview/latest.json`);`npm run test:page` 已验证完整 iframe、结构导航、当前单元发送和定位批注(17/17,`test-results/caogen-deep/2026-07-10T12-48-21-376Z/page-operation-smoke.json`)。发送给 Agent 的仍只有提取文本、元数据和批注,视觉 data URL 不会进入提示词。系统渲染可能与原应用中的完整原版式存在差异;编辑、复杂公式、动画和像素级一致性仍未完成,不得宣称。
- 3D 办公口径更新:Office model 已从真实 `SessionState` 派生路由决策、Provider/密钥故障切换、预算/成本、最近耗时、审批、工具、子任务、worktree 隔离/分支/状态与 checkpoint 文件变化;同 Provider 密钥接管会进入选中 Agent 信号栈并点亮工位故障恢复指示,只显示 key 标签。OfficeView 打开时会对可见会话按需刷新 `git status`,并把分支、dirty 文件数、staged/unstaged/untracked、错误状态汇入顶部指标、选中 Agent 面板和工位低位 3D 指示条。设施区离席 Agent 点击验证改为真实 canvas 投影路径扫描,不会直接调用 React/store 状态;最终实现已由两次连续单跑和完整 Deep 内的 orchestration E2E 连续 3 次通过,并覆盖工位、审批 Agent、设施 Agent 与会话打开链路(`test-results/caogen-deep/2026-07-19T16-02-28-655Z/deep-test-report.md`)。由 `npm run test:office-status-recheck`、`npm run test:provider-key-failover` 覆盖;Electron 页面流 `npm run test:page` 也已验证真实会话/worktree/Git 状态、可点击工位和非空 3D canvas。当前不宣称全量实时 git diff 轮询、完整项目交付驾驶舱、长期趋势图或可替代发布管理系统。
- 3D 办公性能口径更新:Office chunk 会在应用首帧后预取,进入 Office 后按 Boot → procedural Low → 选中 Agent Full 分阶段挂载;12 Agent 场景保持 `1 Full + 11 Low`,未选中 Agent 的 Low 不加载 GLB/Draco。历史基准（干净提交 `488caaa5`）记录过 14 项 required 检查及 macOS x64 12 Agent Auto 冷路径 shell/Canvas `26.6ms`、可交互 `170.9ms`、Low `350.0ms`、后台 Full `1310.5ms`;该历史 report 未随当前 checkout 保留。2026-07-18 当前 dirty-worktree 重跑写入 `test-results/office-performance/2026-07-18T09-13-54-270Z/report.md`，因 Electron page target not found 未形成通过证据；因此这些历史毫秒数和当前失败都不替代目标机器上的可复验 release gate。
- 水墨数字员工基础继续推进:七岗位/七状态已抽成 Provider-neutral shared contract;招聘表单把显式岗位写入 `DigitalWorker.avatarProfile`,真实 Electron 已验证 `review-test` 选择、卡片来源、持久 JSON 与三次启动恢复。OfficeModel 从真实 Session 派生 idle/thinking/tool-running/awaiting-approval/blocked/delivering,并只对绑定 `workflow-repair:<SHA-256>` canonical WorkItem 的活动 Session 投影 repairing。Office 已通过不可变 DigitalWorkerBinding 解析岗位,透明 2.5D Three.js Sprite Rig、机器人回退和显式 alpha/QC 登记门禁已接线。七角色母版、Idle、Tool-Running、Awaiting-Approval 与 96px/48px 专家 QC 已落盘;其余 28 张状态图、49 张透明运行时派生、机器人主角色退场和真人盲测仍未完成。28 个剩余 `gpt-image-2 edit` 命令已按 1024x1536/high/PNG dry-run 28/28;统一透明衍生 Prompt 与 49 项 digest 作业合同已冻结,生产预检确认 7/7 母版、42/42 状态 Prompt 有效并如实显示 21/49 源图。21 张纸底离线透明候选虽通过结构门禁,但暗底拼图发现接触阴影,因此未注册。资产门禁首张 PNG 的 CRC 表初始化缺陷已修复,新增 valid RGBA/corrupt CRC 双向 smoke。`npm run typecheck`、`npm run build`、`npm run test:office-status-recheck` 23/23、招聘 policy/Domain/真实 Electron 11/11、canonical 三故障点强杀恢复和 Acceptance repair/retest 均通过;正式目录 `npm run test:watercolor-assets` 仍如实报告 0/49 incomplete,发布门禁 `test:watercolor-assets:required` 尚不能通过。
- Assistant/Studio 性能口径更新：clean `1675eb50` 的真实 Electron required 报告 `test-results/assistant-studio-performance/2026-07-28T05-20-41-787Z/report.json` 在参考设备 `MacBookPro16,1`（Intel i9-9980HK、32 GiB、macOS 26.5.2、Electron 40.10.2）覆盖 `1320x860 / 760x700 / 360x520`，cold 3 样本 P95 `31.4ms`、warm 60 样本 P95 `32.4ms`，原 `<300ms` 门槛不变；完整 Project/Goal/WorkItem hydration 为独立诊断 `1204.5–1516.9ms`。门禁只允许首个调度污染、Studio 数据就绪超时或有效 cold 超阈值在全新 renderer/userData 重试一次，失败 phase、aria-busy、Project ID、Goal/WorkItem IPC 数量、页面错误和查询耗时必须保留，连续失败仍阻断；本次 clean run 三个 viewport 均首试通过。
- Assistant/Studio 性能口径更新:`NFR-PERF-001` 已由当前 dirty worktree 的真实 Electron gate 在参考设备 `MacBookPro16,1`（Intel i9-9980HK、32 GiB、macOS 26.5.2、Electron 40.10.2）覆盖 `1320x860 / 760x700 / 360x520`。最终当前源码报告中 3 个 fresh-process cold 样本 P95 `156.9ms`、60 个 warm 样本 P95 `238.1ms`，三个视口 cold/warm 均严格 `<300ms`；切换期间本地 Provider 响应保持挂起，每个视口始终只有一个 Session、一个 canonical Run 和一个模型请求。实现通过稳定移动侧栏回调、缓存 Sidebar/Studio/Workbench 重型根和把纯 CSS 模式投影上移，减少与模式无关的重渲染；真正消费模式的 Composer、状态栏和路由消息仍正常更新。证据见 `test-results/assistant-studio-performance/2026-07-28T19-59-33-903Z/report.json`；同机高负载期间存在保留的失败样本，不能把单次 targeted pass 扩大为 release 硬件普适结论。
- 五支柱当前判断:多厂商、调度和 3D 已形成可用优势;迁移级工作流与长期自主执行仍受真人 N1、跨 Provider 账本、后台持续运行和交付证据约束。当前没有统一评分工件,不再给出百分比。
- 用户实测反馈已修 4 项(冗余"你"标注、矛盾错误文案、引擎×Provider 404、填 key 不生效)

- Provider Profile 可逆迁移的本地产品切片已实现到可恢复写入与当前 Electron UI 边界：无凭据导出、严格导入/冲突预览/逐项决策、目标变化后的凭据隔离、keyed→no-auth 真删除、脱敏私密备份与回滚均保留；Store 写入使用跨进程 mutation lock、锁内 CAS、临时文件 fsync→rename→目录 fsync。schema v2 operation journal 对 import/rollback 记录 `prepared / waiting_reconciliation / committed / aborted`、前后 Store digest，以及 safety/source backup ID + frozen digest；启动只做分类与对账，不自动重放 Store 写入。最新 dirty merge commit `8ba60148` 的无 GUI gate 为 `135/135`（`test-results/provider-profile-smoke/2026-08-01T02-03-32-180Z/report.json`，worktree count `444`），新增活动 Key 标签/数量与目标凭据绑定变化断言，并保留凭据排除、session-only Key 脱敏迁移、journal 文件边界和 backup 文件名/内嵌 ID 不一致拒绝。真实跨进程/`SIGKILL` restart gate 为 `13/13`（`test-results/provider-profile-restart/2026-08-01T02-03-32-195Z/report.json`，worktree count `444`），覆盖存活 owner 竞争时精确 `LOCK_HELD`、失败 candidate 清理、同进程可重入、正常释放竞争最终获取、import/rollback 在 prepare 后与 Store commit 后强杀、死进程锁回收、二次恢复字节稳定/零自动 replay，以及同进程对账后 cache reload 与普通写入恢复。`prepared`/waiting 期间 6 类普通 writer 和伪造 operation ID 均不能写入第三 Store digest；未知 Store digest 会持久进入 `waiting_reconciliation`，Store 修复为 before/desired digest 后分别收敛为 `aborted`/`committed`。safety/source backup 会在 Store commit 前和 terminal 前复核 ID/digest；篡改在 prepare 后或 Store commit 后均先阻断 terminal，恢复冻结字节后才分别收敛并重新开放普通写入。当前 Electron required gate 为 `54/54`（`test-results/provider-profile-e2e/2026-07-31T18-36-51-341Z/report.json`），通过真实主进程 IPC、Renderer 和四张截图覆盖危险 URL 在预览前拒绝、导入预览/应用、跨启动回滚、脱敏备份/导出、活动 Key 标签与凭据影响说明、Key 删除取消/确认/重新录入，以及 `760x700` 无水平溢出；备份文案已明确 API Key 不写入 Profile 备份。Provider Profile Service、operation journal、mutation lock 与 Store repository 已进入 durable writer inventory；最新 dirty report `test-results/durable-write-inventory/2026-08-03T14-22-17-696Z/report.json` 通过 `13/13`、登记 76 个模块和 432 个 sink call。Permission Audit 与 Conversation Ledger 均已推进为 `implemented_unverified`；后者现在对 canonical append 执行文件 fsync，对 replace/copy 执行 candidate fsync、原子 rename 和 POSIX 目录 fsync，严格写路径拒绝损坏账本，receipt 仅作可重建投影。inventory 因此降至 7 个 recovery gap 和 7 个显式 schema gap；Windows 目录 durability/ACL 仍未形成同等级证据，Provider Store 顶层 schema 也仍未版本化，所以 `NFR-REC-002` 继续是部分完成。Windows ACL、真人迁移计时、真实 Provider 模型发现/健康/failover 和 clean release 绑定仍未完成；这些证据绑定 dirty worktree，不是 release-ready，M2-T10/AC-20 保持开放。
- Provider Runtime containment 新 required gate `test-results/provider-runtime-containment/2026-08-01T01-58-13-107Z/report.json` 以 `97/97` 通过：Broker record 不含明文、session-only Key 无法被 fresh process 恢复；仅 DAG 拆解器保留一个直接 raw resolver consumer，OpenAI/Anthropic 网络路径改用 scoped Provider credential lease；动态 `import/require` 绕过会被拒绝；全部 IPC/preload/Renderer 文件不可引用 server-only Provider/Broker/credential-runtime 类型，Provider IPC 只返回精确脱敏 `ProviderView`/Key metadata。七个 Provider channel 还逐项绑定目标函数、精确返回类型、根委托和恰好一次调用；对抗性 AST 与 TypeScript 可报告的可达性负例会拒绝嵌套、序列、部分控制流、不可达、重复或非最终目标调用。该 gate 使用确定性 secure-backend test double，不证明平台密码学强度；provider/project/session/operation/expiry scope、所有子进程最小环境、全输出 secret canary 与 clean release 绑定仍开放，因此 TRUST-006 仍只是“当前已验证（基础）”。
- 2026-08-01 在当前 merge 后源码（含 OpenAI/Anthropic scoped Provider credential lease）重跑 Provider Profile 三阶段 Electron gate：`test-results/provider-profile-e2e/2026-08-01T02-08-04-853Z/report.json` 以 `54/54` 通过，覆盖真实主进程 IPC、Renderer、四张截图、Key 标签/凭据影响、跨启动回滚、脱敏备份/导出、Key 删除确认/重新录入与 `760x700` 无溢出；报告绑定 `8ba60148`、dirty worktree count `444`。这证明当前源码的本地 UI slice，不替代 clean release、Windows ACL、真人迁移或真实 Provider 发现/健康/failover。
- Codex / Claude Code 资产迁移的产品切片和自动化证据已完成：设置页不再要求先创建项目，用户清空默认项目目录后会保持为空，可直接按“对话”入口扫描用户级资产；规则、Skill、MCP、Hook 和通用配置分项展示，Codex TOML 结构化解析，Provider/模型/认证不写入规则，Hook 不自动启用。MCP 环境变量、Header、凭据参数、URL userinfo/query/fragment 与未知字段会被剥离；风险默认、显式覆盖、SHA-256 扫描绑定、来源/目标符号链接拒绝、原子批量恢复和一键回滚均已接入。当前源码 `npm run typecheck`、`npm run build`、主集成 `43/43` 和真实 Electron `15/15` 通过，UI gate 同时验证 `1320x860 / 760x700 / 360x520` 迁移页无横向溢出，报告为 `test-results/assistant-studio-ui/2026-07-29T02-15-06-692Z/report.json`；私密配置合同、报告脱敏和 worktree/history secret scan 通过。编码规范 required 审计仍为 `17 pass / 5 warn / 15 fail`，15 项均是既有基线在本轮拆分后改善或内容变化所触发的 `baseline-refresh-required`，本轮没有刷新基线，因此该门禁不记为绿色。以上均绑定 dirty worktree 且只证明合成自动化，不包含付费 Provider 调用、真人 30 分钟迁移、完整 Artifact/Acceptance 主链或 clean release 绑定；M2-T9、W1 与 AC-19/20 继续开放。

# 2026-08-10 Code Workbench Delivery

The current source now includes the TypeScript cross-file refactor workflow in addition to the debugger, test runner, multi-tab editor, LSP and diagnostics slices. The refactor path now writes an atomic, integrity-checked private journal before cross-file mutation; fresh-process reconciliation compensates interrupted apply/rollback operations only while every file remains on a frozen before/after digest boundary, preserves completed rollback across restart, retries a blocked recovery after external drift is repaired, and never overwrites an unknown file state. Every transition rechecks the expected digest immediately before writing. Completed rollback windows expire without blocking normal later edits, terminal records are pruned within 50 records and 250 MB, stale temporary records are cleaned after 24 hours, unresolved/corrupt saturation fails closed, and Renderer recovery responses contain only project-relative file names plus generic messages rather than source snapshots or absolute workspace paths. Final local gates are green: refactor smoke `36/36`, refactor Electron `12/12`, debugger `16/16 + 13/13`, project tests `26/26 + 11/11`, file editor `31/31 + 29/29`, diagnostics/language intelligence, and typecheck. These are dirty-worktree development proofs, not a 1.0 or release-ready claim.

The crash-journal/restart-recovery foundation and a 5,201-file synthetic large-repository indexing foundation are now implemented and covered locally. `IDE-001` and `IDE-002` remain open for representative 50,000-file/multi-language monorepo performance, full keyboard coverage, and a real heavy-user golden workflow. A new isolated unpacked preview will be generated from an exact clean source commit before user evaluation.

The large-repository indexer increment removes the old 5,000-row file-search blind spot, replaces the directory `shift()` queue with a linear cursor and 64-way bounded metadata batches, reports 50,000-file truncation explicitly without deleting unseen warm-index rows, and avoids a full fallback scan when ripgrep successfully returns zero matches. SQLite snapshots are now serialized, debounced, atomically replaced and flushed before disposal; 24 watcher additions coalesce to at most three snapshots and survive reopen. The latest Windows x64 synthetic gate indexes 5,201 TypeScript files/10,402 symbols with one snapshot: cold `3408.8ms`, warm `274.8ms`, substring file query `51.8ms`, full-index fuzzy query `26.8ms`, symbol query `4.6ms`, and no-match code search `61.2ms`. Evidence is `test-results/indexer-large-repo/2026-08-10T16-27-09.676Z.json`; it contains counts/timings only, not temporary paths or source. This stacked dirty-branch evidence does not prove the final 50,000-file ceiling, mixed-language monorepos, network filesystems, sustained watcher storms, Electron interaction latency, or a clean release commit.

The Provider visual editor now exposes provider-scoped failover, retries, first-byte/idle/request timeouts, circuit-breaker thresholds, and endpoint priority instead of requiring JSON edits. Production save/reopen and compact UI pass `18/18`; runtime consumption passes timeout `15/15`, circuit breaker `11/11`, endpoint target `3/3`, request overrides `17/17`, and CC Switch import `37/37`. This improves configuration parity but does not close real-provider authorization, billing reconciliation, or long-term usage accuracy.

Provider billing provenance is now explicit across the complete selected interval: `reported`, `provider-pricing`, `builtin-pricing`, `imported`, and `unpriced` are counted separately. Unpriced requests retain request and Token evidence but contribute zero currency, with an explicit warning that the displayed total may be lower than the Provider's final bill. Backend and static Dashboard gates pass `46/46 + 27/27`; the real Electron desktop/reopen/filter/compact flow passes `36/36` at `test-results/provider-usage-dashboard-e2e/2026-08-10T01-52-52-842Z`. The isolated unpacked preview is `dist-preview-billing-provenance-20260810-101221/win-unpacked/CaoGen.exe`, SHA-256 `69C27565207CA45DCB89FCA445C2FCD998BB36FB8AABC2E8708D68CD6AD66327`; renderer first launch/restart passed at `test-results/windows-unpacked-renderer-smoke/2026-08-10T02-36-29-539Z`. This is a dirty-worktree local-ledger proof, not external invoice reconciliation or long-term paid-account accuracy.

Provider billing reconciliation foundation is now implemented. CaoGen stores only Provider id, billing period, USD amount, and a fixed source enum in a schema/revision/digest-bound atomic store; it rejects symbolic links and corrupted statements. Reconciliation returns `matched/mismatch` only for complete, untruncated, fully Provider-reported usage and otherwise returns `incomplete` with explicit reasons. Authorization quota observations also survive restart without persisting redeemed access tokens. Gates pass: billing `34/34`, authorization `129/129`, usage `48/48`, Dashboard static `32/32`, real Electron `42/42`, durable-write inventory, Effect-entry required audit, typecheck, and production build. UI evidence is `test-results/provider-usage-dashboard-e2e/2026-08-10T03-07-19-468Z`. The new isolated preview is `dist-preview-billing-reconciliation-20260810-110846/win-unpacked/CaoGen.exe`, SHA-256 `AAC59991FA4DAF30253F1D44CCFA06CBBCBC127D123C883289F097D9A8DC9A67`; renderer first launch/restart passed at `test-results/windows-unpacked-renderer-smoke/2026-08-10T03-10-14-779Z`. Automatic Provider invoice/balance ingestion and long-term paid-account accuracy remain open, so this does not close the 1.0 release gate.

# Current Focus

**当前焦点是 `PKG-001` 准备与竞品替代功能开发。** `FIX-000` 已由同一 D0 SHA 的 portable、installed、Owner 12 步和 clean-uninstall 证据关闭；不再重复要求 Owner 安装、UAC 或卸载确认。当前工作树仍含大量未提交增量，因此只能生成隔离 unpacked 开发预览，不能冒充 clean C1、安装候选或 1.0。公开 v0.1.7 完整性、正式跨平台 Release Doctor、签名矩阵、真实付费 Provider 和 M1 陌生用户仍各自开放。

# Goal

**北极星 N1**:真实重度 AI 工作者 **30 分钟内**跑通日常主链路(导入资产→建会话→@文件/资料→执行任务→审结果→提交/交付),资产零丢失。以五支柱代差做成"世界第一 / 中国首创"验收方向的多厂商 AI 工作桌面。

# Next Milestone

**R0 Truth + Owner Preview（2026-08-14）** — Definition of Done：Acceptance Map 无遗漏/重复；20 个黄金工作流与证据分类完成评审；Windows 完成“精确 SHA 安装测试 -> 缺陷回流 -> clean 候选 -> 原场景 Owner 复测”闭环，且无 Critical/High 阻断。

1. 先解决主线 0.1.8 与 autostash 0.1.7 策略之间的 release identity，最终候选提交 worktree clean，`package.json` 与 lock 根版本一致
2. `test:deep` 在该精确提交上 required 全绿；optional skip 保持显式，不算 pass
3. macOS x64/arm64 候选资产完成 Developer ID 签名、notarize、staple、Gatekeeper 和 packaged-app 原生启动审计
4. Windows x64 仅允许文件名与说明均明确标注的 unsigned preview，并在原生 Windows 完成安装、启动、卸载与清理审计；它不进入稳定更新通道，也不计入正式矩阵
5. Release Notes 只写当前已验证能力，平台限定 final gate 绑定实际上传资产通过；正式跨平台 Doctor 继续保持 blocked
6. 当前 v0.1.7 公开 Release 的资产/正文漂移经明确授权后恢复或按新合同重新审计；未经授权不得删除资产、改正文、创建 tag 或发布候选
7. 一名非项目参与者从官网取得受支持且已审计的资产，不绕过平台安全检查完成安装、Provider 配置和第一个只读任务，并保留时间、失败点与恢复动作
8. 将卡点转成 onboarding 修复或明确 issue；该单人结果只关闭 M1，不冒充 M2 的三人 N1、真实 Provider release record、1.0 stable 或正式跨平台 ready

# Priority Tasks

**P0**
- 用户反馈快修循环(常设)
- Release identity 与平台限定候选：`03c3fee2 / 30243108279` 的 v0.1.8 Intel 签名资产及 scoped preflight 只保留为历史证据；主线 clean 源码后继 `1675eb50` 已完成 exact clean Deep `171/171 required`，但没有对应签名候选。autostash 中的 v0.1.7 平台策略只绑定 dirty worktree。必须先锁定最终版本与 clean commit，再刷新 Deep、macOS x64/arm64 正式分发、Windows x64 unsigned preview 原生审计、独立资产核验和平台限定 Notes/preflight；期间不得发布
- 恢复 v0.1.7 公开 Release 到批准的 Intel 五资产与仓库最终正文；当前自动审计 fail-closed，删除 3 个 Windows 资产及改回正文等待创始人明确授权
- ~~v0.1.7 Intel 签名楔子版发布:clean candidate、Deep、macOS x64 签名公证/安装/启动审计、Release Notes、当时的公开资产审计和官网同步~~ ✅；Apple Silicon/Windows 不在该历史发布范围内，后续平台限定策略须重新取证
- M1 首位陌生用户 Quick Start 验收；随后按 M2-T1 扩展到 3 名真实同类工具用户的 N1 30 分钟迁移计时
- ~~arm64 / universal 打包~~ ✅ 已发布至 v0.1.1
- P0-1B:~~接入 `search_replace`、OpenAI/Anthropic 原生文件编辑的 queryable file Effect~~ ✅;~~E2A 接入 Renderer worktree patch、独立 push→PR 与 Agent `git_create_pr`~~ ✅;~~E2B-1 接入 Renderer 文件保存/commit并阻止复合 Code Forge commit/pr~~ ✅;~~E2B-2 接入 Renderer stage/stageAll/unstage/accept hunk 的精确 Index CAS~~ ✅;~~E2B-3 接入 discard hunk 独立文件 Target 与强杀对账~~ ✅;~~DAG autoMerge patch Effects 与 completion/finalizer durable outbox/receipt 接入并确认~~ ✅;~~managed-worktree create/remove 生命周期 Effect~~ ✅;~~建立 156 IPC、52 Agent 工具、79 嵌套动作的版本化高风险入口清单和 required drift gate~~ ✅;~~将 `attachments:copyImage` / `attachments:saveImageBytes` 接入 opaque `attachment_write` Effect 与强杀对账~~ ✅;~~将 `projectContext:write` 接入 queryable `file_content` Effect 与强杀对账~~ ✅;~~将 `plugins:probeMcp` 接入 opaque `mcp_probe` Effect 与强杀对账~~ ✅;~~将 `plugins:installLocal` / `plugins:uninstall` 接入 queryable managed-plugin Effect、强杀对账与真实 Electron IPC E2E~~ ✅;~~将 `terminals:start/write/resize/close` 接入 opaque `terminal_action` Effect、强杀对账和恢复面板~~ ✅;~~将 `browser:open/navigate/back/forward/reload` 接入 opaque `browser_navigation` Effect、强杀对账和恢复面板~~ ✅;~~将 `migration:import` 接入 opaque `migration_import` Effect、强杀对账、隐私断言和恢复面板~~ ✅。Issue、消息、可查询 MCP 与 Code Forge patch 已有较新源码增量；未完成批次末编译、系统/强杀回归的部分不得标记完成
- P0-1C:TaskRun Effect evidence v6 foundation、v9 Workflow/Conversation Ledger、canonical recovery sessions、`legacy / compare / canonical` 恢复读源和可逆 migration/continuity 门禁已落地；Conversation Ledger 已按 stream/generation/event 独立归档、启动回填、DB fallback 恢复和 Project 授权删除接入。Responses 上下文持久游标、服务端链失效后的有界跨协议重放、附件与 tool-call/result 配对、显式 Provider/模型分叉，以及显示 causation/correlation、Checkpoint、generation、Effect lease/fence/evidence 的恢复 UI 已实现。v9 新增部分仅完成类型检查；下一步继续全入口接入、完整 Artifact Graph/blob/sourceRef 生命周期、集中恢复/强杀/跨协议验证、统一保留/导出/导入和生产补偿计划/审批/执行
- P0-2A（部分完成，dirty worktree）：旧的持久化“全局 GUI 五分钟授权”已失效；GUI grant 仅由主进程在当前权限请求上签发，绑定 session、项目 cwd、精确 `gui_*` action 与完整输入 digest。权限设置页已改为版本化结构化规则编辑器，并对旧 allow/deny 文本字段做幂等迁移后清空兼容字段；持久规则现可用 AND 组合匹配工具、项目路径、命令、网络 hostname、GUI process/window、MCP tool、MCP 非敏感标量参数、有效 GUI postcondition 和风险条件，旧规则迁移时新语义字段保持中性。MCP 参数范围使用成对的 RFC 6901 pointer 与 wildcard 值模式，对无效 pointer、对象/数组值及凭据类字段名 fail-closed；`mcp_call_tool` 与动态 `mcp__server__tool` 均保持 high risk，动态工具名或参数漂移不会命中持久规则，Full Access 也不能静默放行。非 GUI grant 也已进入同一内存控制面，只允许 `write_file/search_replace/edit_file`、四类 Office 产物、`bash/git_stage/git_commit` 的稳定目标，并绑定 session、cwd、精确 tool、完整 input digest 与 frozen Effect target digest；审批卡同时显示输入摘要和 Effect/diff scope。high/critical、MCP、push、Issue 创建和通知不能通过 Full Access 静默放行，也不能获得五分钟 grant。两类 grant 均在进程重启、会话关闭、到期时失效，可在设置页查看和单独/全部撤销；GUI 总开关关闭还会立即撤销 GUI grant。`gui_activate_window/click/type/scroll/hotkey` 已支持绑定明确 window/element selector 的有界 postcondition，目标不稳定的 hotkey 仍只能逐次审批。当前 `test:gui-permission:required` 为 `40/40`，`test:gui-windows` 为 `11/11`，`p0-004`、MCP client、durable direct-write `40 checks`、typecheck 与 production build 通过；`p0-004` 同时覆盖命令/主机/GUI/普通与动态 MCP 工具及参数语义漂移和非法规则 fail-closed。真实 Windows Notepad observe-act-verify 报告仍为 `test-results/gui-input-preflight/2026-08-07T10-48-58-095Z/report.json`；最新完整 page smoke 为 `21/21`，报告 `test-results/caogen-deep/2026-08-07T12-47-31-473Z/page-operation-smoke.json`，桌面与 390px MCP 参数规则编辑器截图均通过无溢出检查。复合多 Effect 工具、截图式 postcondition、失败恢复策略、macOS/真实 installed E2E 与 clean release 绑定仍开放，因此不得标记 P0-2A 完成
- P0-2A 复合能力增量（2026-08-07，dirty worktree）：规则 schema 已升级为 v2，并由权限系统与 DigitalWorker 共用 `workspaceRead/workspaceWrite/terminal/browser/network` 分类器。allow 必须覆盖调用所需的全部能力；deny 与任一能力重叠即阻断；v1 allow 的空能力范围不会自动放行，旧 deny 仍保持拒绝。`bash`、交互 GUI、`mcp_call_tool` 和动态 `mcp__server__tool` 均按五能力复合调用处理，审批卡与审计记录携带主进程计算的实际能力集合。`p0-004` 已覆盖 terminal-only 不得放行 bash、network deny 阻断 bash/MCP/GUI、v1 allow fail-closed 与动态 MCP 不逃逸；DigitalWorker policy `122 checks` 同步锁定普通与动态 MCP 的五能力分类；最新 page smoke `21/21` 报告为 `test-results/caogen-deep/2026-08-07T13-05-23-425Z/page-operation-smoke.json`，桌面双列与 390px 单列能力编辑器均通过无溢出检查。上一行仍开放的“复合多 Effect”特指一次工作流跨多个 `EffectTarget` 的账本、对账与补偿，不是本次已完成的单次调用能力 envelope；P0-2A 仍为部分完成，`FIX-000 / No-Go` 不变。
- ~~P0-4A:把深测改为 `pass / skip / blocked / fail` 四态,required 项不得以 skip 通过~~ ✅
- ~~P0-2A:移除新 Provider Key 的 `b64:` 写入 fallback,建立进程内 session-only 降级、旧数据安全迁移和 Provider Broker 基础~~ ✅
- P1-10 截图式 postcondition 增量（2026-08-07，dirty worktree）：`gui_activate_window/click/type/scroll/hotkey` 现支持 `visual changed/unchanged`。运行时在动作前把 UIA 窗口唯一解析为 Electron window source 并冻结 `sourceId`，动作后只轮询同一源；空帧、非唯一源、源漂移、尺寸漂移、取消和超时均 fail-closed。`changed` 使用最小变化像素比例，`unchanged` 必须在完整等待窗口内持续低于最大比例；结果只返回尺寸、比例和 SHA-256 摘要，不返回截图或像素。权限规则的 `requirePostcondition` 已接受与动作共享精确窗口 selector 的视觉断言。`typecheck`、production build、`p0-004`、GUI required `44/44`、Windows GUI `11/11` 通过；真实 Windows Notepad 已完成“内存基线 -> 定向输入 -> 同源视觉变化”验证，最新通过报告为 `test-results/gui-input-preflight/2026-08-07T13-35-44-880Z/report.json`；此前 `2026-08-07T13-35-06-277Z` 的旧 window-absent 检查曾单次超时，失败证据保留。失败后的恢复/补偿、macOS、installed E2E 和 clean release 绑定仍开放，因此 `FIX-000 / No-Go` 不变。
- P0-2B:完成 provider/project/session/operation/expiry 作用域、子进程最小环境、全出口 secret canary 与数据保留/导出/删除策略
- P0-3:建立 MCP/插件 Capability Manifest、固定版本/digest、最小环境和恶意 fixture 隔离门禁
- 凭据安全:所有疑似泄漏或曾经外发/公开上传的个人/仓库 token 必须在对应平台轮换或撤销;仓库和 GitHub Releases 内不得保存真实密钥、webhook、证书、keystore、provision profile、签名材料或本地证据包

**历史交付批次 P1**(2026-07-06 收口:3/3 保留项完成;不同于竞品差距文档的新 P1 路线)
- ~~插件治理下半场:安装 / 卸载 / 版本 / 权限声明~~ ✅(本地安装+回收站卸载+
  路径牢笼,7 断言冒烟;版本锁定降级为版本展示,市场分发本版不做)
- ~~会话全文搜索(U5.1)~~ ✅(侧栏消息内容命中直达会话)
- ~~worktree 冲突三栏 + 合并回执~~ ✅(三栏对照+patchSha256 回执)

**历史交付批次 P2**(2026-07-06 推进:3/5 已推送;不同于竞品差距文档的新 P2 路线)
- ~~聊天头工具栏图标化(U3.3)~~ ✅ 8 按钮→图标+⋯更多下拉;page-smoke 按 aria-label 适配全绿
- ~~chat 历史自动压缩~~ ✅ 超 48k token 摘要旧段,不切断 tool_call 配对(e2e 4/4)
- ~~Responses 协议接工具循环~~ ✅ 官方 OpenAI 模型也成真编码 Agent(e2e 5/5)
- ~~路由能力表自学习~~ ✅ 按实测成败/延迟给同档模型打平降权(集成 T17 验证)
- N1 迁移实测:向导映射✅、演练 fixture+计时脚本✅(docs/N1-MIGRATION-DRILL.md);真人 30 分钟计时仍未做，formal 1.0 stable 的 N1 硬门禁仍开放

**Work OS Phase 2 并行任务**(2026-07-08 新排期)
- B0 Release Gate:保持 README/STATUS/release notes 与真实 gate 一致,审计公开 GitHub Release 资产,最后合并;草稿门禁见 `docs/RELEASE-GATE-DRAFT.md`
- B1 Windows GUI Required:P2-001 strict VS Code GUI/cross-app/input evidence,由专门 Windows agent 后续补
- B2/B3 IDE 插件链路：已于 2026-08-07 退役；历史 evidence 保留但不再作为当前产品或发布能力
- B4 China External Evidence:6 Provider tool-call parity 已 72/72、`maxGap=0`；P2-004 的独立 China real-network evidence 与发布范围声明仍需按门禁处理

# Blockers

**本地发布门禁与外部条件:**

| 阻碍 | 等级 | 状态 |
|---|---|---|
| ~~32 并发压测 5/6~~ | High | ✅ 已修:并发闸门(8 在途)+ 瞬时重试;连跑 3 次 7/7 error=0 |
| ~~最新 dist:mac 卡 Electron 下载~~ | Medium | ✅ 已修:.npmrc 配 npmmirror;双架构 DMG 完整产出 |
| ~~v0.1.7 历史 Claude Runtime auth 误判~~ | High | ✅ 已从该发布包删除外部 Runtime 与登录检测；后续源码的显式 Claude 专项不改变默认路径门禁 |
| ~~窄屏响应式布局未过人眼复核~~ | Medium | ✅ 已修:同日 responsive smoke 覆盖桌面/窄屏暗色与浅色主题及水平溢出;证据:`test-results/caogen-responsive/2026-07-06T10-08-05-301Z/responsive-light-smoke.json` |
| ~~新最终候选 Deep~~ | High | ✅ run `30162696430` 在 clean `bbec5265` 上通过 `157/157` required、2 optional skip，并完成 Intel 签名/公证/安装/renderer 与五资产证据；optional skip 未计作 pass |
| v0.1.7 公开 Release 完整性 | High | ❌ 远端现为 8 资产且正文包含未批准 Windows 发布声明；仓库五资产 Notes 合同审计失败，等待明确授权恢复远端状态 |
| v0.1.8 Intel 历史候选 | High | `03c3fee2 / 30243108279` 只保留为历史证据；当前先解决 release identity，再按最终版本与平台范围生成新的 clean candidate、独立资产核验和 Notes/preflight |
| arm64 包真机启动 | High | 历史 v0.1.7 Intel 发布未包含 arm64；较新的平台限定策略若纳入 arm64，必须在真实 Apple Silicon 上补齐构建、安装、启动和 required audit，Intel 不可替代 |
| Docker | — | 不需要；产品运行模式、资源和分支已删除 |
| Claude 登录 | — | 默认 OpenAI-compatible 与原生 Anthropic Messages 路径不需要 Claude 登录；只有用户显式选择可选 Claude 专项时才按其独立凭据合同运行，不阻塞默认启动或发布门禁 |

**外部与人工发布门禁:**

| 阻碍 | 等什么 |
|---|---|
| Apple Developer / 签名材料 | 历史 Intel GitHub lane 已用已配置 secrets 完成 Developer ID、公证和 staple；当前本地进程只有 x64 签名基线、没有可用于最终候选的完整公证配置。最终 clean candidate 必须重新注入受控凭据并生成 notarize、staple、Gatekeeper 与原生启动证据 |
| GitHub Actions 发布 secrets | 历史 Intel lane 已验证可用，但不能自动继承到新的版本/提交；Apple Silicon lane、当前候选绑定和聚合资产仍需重新验证，workflow 只生成只读证据且不发布 |
| Apple Silicon 真机 | 只有平台限定候选要宣称 arm64 正式分发时才需要；一旦纳入范围就必须提供真实 Apple Silicon 构建、安装和启动证据，Intel 机器不能替代 |
| Windows 正式签名 | 当前不阻塞经过原生审计并明确标注的 unsigned preview；正式跨平台 Gate 继续 blocked。采购并配置 Authenticode 后仍须生成时间戳、安装和 12 资产矩阵证据才能关闭 |
| 指定 Provider key / 额度 | 6 Provider tool-call parity 已 72/72；formal 1.0 仍需在最终 clean candidate 上生成真实默认 OpenAI-compatible release record，覆盖 send/tool/artifact/recovery/usage/billing |
| 凭据轮换 | 曾暴露或疑似外发的 token 必须由凭据持有人在对应平台撤销/重建 |
| N1 30 分钟计时 | formal 1.0 stable 硬门禁；需真人按秒表跑并留证 |
| M1 首位陌生用户 | 验收指南、模板、审计器与负向 smoke 已就绪；仍需 1 名非项目参与者在 Intel Mac 上完成私有实测 |
| 后续版本 push / GitHub Release | 每次仍需在精确 release commit 上通过发布门禁并获得用户授权 |

# Decisions

不会改变的原则:

1. **实测才算完成**:每个特性配真实 E2E(真进程/真 IPC/真模型调用),"编译过"不算数;状态如实标注,不虚标
2. **六环链路**:新能力必须主进程 → IPC → preload → types → store → UI 全通才算接通
3. **不搬同类工具代码**:只借鉴信息架构与交互,纯自实现
4. **安全边界**:新密钥不得以明文或可逆编码持久化且不出主进程;旧可逆记录必须迁移或要求重新输入;文件工具路径牢笼;权限审批不可绕过(bypass 需显式选择);发布物不含任何凭据
5. **中英双语**:所有 UI 文案 zh/en 齐备,zh 为母语级
6. **每任务独立提交**,提交信息写"做了什么 + 怎么验证"
7. **诚实降级**:能力不可用时如实报告(如 OCR 无引擎、PR 无 gh/glab),绝不伪造结果

# Out of Scope

当前滚动发布周期明确不做:

- 云端 Routines / 云端 Runner(本地定时任务已有)
- App Store 上架(走 GitHub Releases 分发)
- Windows 正式签名版不在当前滚动发布周期；当前仅允许原生验证后、文件名和说明均明确标注的 unsigned preview。Linux 打包配置存在但未完成发布验证,不承诺
- 移动端、自研/微调模型
- 插件市场(安装/治理做,"市场"不做)
- 写实游戏级 3D 自由漫游

# Risks

1. **零外部用户数据**:所有"可用"结论出自 E2E 与自测,N1 从未真人验证 —— 最大未知
2. **分发摩擦、发布漂移与候选错绑**:v0.1.7 Intel x64 的 provenance、Developer ID、notarize、staple、Gatekeeper、DMG 隔离安装和真实 renderer 历史证据仍有效，但公开 Release 已偏离批准的五资产/正文合同；在远端恢复或按新合同重新审计前，不得宣称公开资产审计通过。下一平台限定候选仍缺最终 clean artifact 的版本/提交绑定、macOS x64/arm64 正式分发证据和 Windows x64 原生 preview 证据；Windows unsigned preview 可能触发 SmartScreen，必须在文件名、Release/资产说明和下载页明确标注，且不得进入稳定更新通道。陌生用户安装与 Quick Start 证据仍缺，任何平台都不得由其他架构或未批准 artifact 替代
3. **外部凭据轮换状态不可由仓库验证**:疑似外泄 token 必须由持有人在对应平台撤销/重建;仓库只保留占位符、环境变量名和脱敏状态
4. **长会话膨胀**:~~chat 历史无压缩~~ 已加自动摘要压缩(超 48k token);OpenAI 引擎工具声明每请求固定开销仍在

# Success Criteria

- **下一次平台范围发布验收** = macOS 正式分发证据全部成立 + Windows unsigned preview 的原生安装/启动/卸载与显式标注门禁成立;正式跨平台 Release Doctor 保持 `not_ready`,不得对外写成跨平台正式发布
- **长期成功** = 北极星 N1 由**非项目相关**的真实同类工具深度用户验证通过(30 分钟计时 + 资产零丢失 + 关键动作无需回退原工具)
- 2026-08-04 Watercolor runtime delivery: all 49 role x state `gpt-image-2 edit` chroma sources were generated. After chroma-key removal and aspect-preserving normalization, 49 1024x1536 PNG RGBA assets are installed under `src/renderer/src/assets/watercolor-characters/` and registered in `VERIFIED_WATERCOLOR_CHARACTER_FILES`. `npm run test:watercolor-assets:required` reports `49/49 verified, 49 registered`; required QC reports `49/49 present, 0 invalid`. Earlier 0/49 interim notes no longer describe the current HEAD. External blind role-recognition review remains a separate visual acceptance step.
- FIX-000 current D0 binding: `593b04a8552461be8cefc08493c24f3699a3aec25c6e262ee05b4e60ccc2ce94`, artifact-set `fce3f68b67c9c7cecdefd26ada748ad8729e89201a7743a124ecec7f6bbdfa84`. This SHA includes the Provider authentication-header fix, restored in-process NSIS install-root removal, complete canonical ownership propagation for managed-worktree Operations, aggregate compatibility for historical ledger-only Operations, Artifact lifecycle ownership rooted in the active Electron user-data store, and confirmed-side-effect replay suppression across Provider failover. It passes the 45/45 preview audit and 44/44 Office delivery required checks. The exact current SHA also passes the required private portable audit, path-bound assisted install, installed Provider/session/Office retest, Studio recruitment, durable restart recovery, default-No cancel/relaunch, confirmed clean uninstall, and the complete 12-step Owner audit with zero findings. `FIX-000` is complete and may advance to preparation of `PKG-001`; the dirty D0 itself is not C1 or a release candidate.
- FIX-000 Owner kit binding: ZIP `d8486f5751a4e2d1f62fd486ef06e72bf2ee1ecc81165383e3d6d017ee5cf284`, manifest `7fdcf56c40bb7dbeea214edf7219467d24e6350419e844c525bfed10ac821eb3`, content-set `0445ee1cfbab283bf18fa12e9e26e5caee7372a5f37eba75914baa6953fcad6d`. The 27.801-minute Owner record SHA-256 is `511e689723246bf507dd9db2dbf22e92a43152ac1c85a61f802480cbef552914`; all 12 steps passed.
- 2026-08-07 Provider authentication-header regression fix (dirty worktree): API-key Providers now resolve an engine-aware default (`Authorization: Bearer` for OpenAI-compatible targets, `x-api-key` for Anthropic), explicit managed header names still override the default, model discovery sends only the resolved managed credential header, and legacy records with no header names migrate on load. The Provider Profile smoke passes `145/145`; typecheck, production build, targeted Provider gates, and the full default P2 suite pass. `test:p2-required` remains failed only on the two explicitly external checks (`china_real_network_required`, `china_tool_call_parity_required`), and release-scope remains ineligible while the worktree changes. The new SHA-bound D0 above contains this fix; fresh installed/Owner evidence is still required, so `FIX-000 / No-Go` is unchanged.
- 2026-08-10 Provider local-version increment (dirty worktree): ordinary Provider create/update/delete now writes a private credential-free before snapshot, while no-op updates create no history. Settings exposes up to 50 local versions with reason labels and a diff-first restore flow showing create/update/delete/unchanged counts and changed field names only; backup bodies, endpoint values, credentials and local paths stay out of Renderer. Preview drift and backup digest changes fail closed; rollback reuses the existing mutation lock, operation journal and reverse safety backup. Provider Profile `155/155`, restart `13/13`, Electron UI `80/80`, typecheck, production build, durable inventory `13/13` and Effect entry required audit (`387` entries) pass. The isolated unpacked preview is `dist-preview-provider-version-history-20260810-1138/win-unpacked/CaoGen.exe`, SHA-256 `F4A7CED68B7CD0DE9BDEA1B3CA14EB53FFE4C05291D3D6D6738D5BFFAE9C50F9`; first-launch/restart diagnostics pass at `test-results/windows-unpacked-renderer-smoke/2026-08-10T03-51-26-811Z`. This is not a clean candidate or 1.0 completion claim.
- 2026-08-10 Provider official-billing-API increment (dirty worktree): the visual Provider editor now configures a same-origin GET/POST billing connector with Provider/no-auth credentials, exact Key labels, Query or nested JSON-Pointer period injection, Unix/ISO formats, bounded metadata and JSON response mappings. Main-process fetch blocks redirects/cross-origin responses, caps bytes/items, accepts USD only, emits bounded error codes, and atomically upserts a `provider-api` statement before automatic reconciliation; URLs, response bodies and credentials do not enter Renderer or Effect targets. Targeted gates pass `62/62 + 9/9 + 34/34 + 25/25 + 48/48 + 34/34`; real Electron end-to-end sync, idempotency and 760px UI pass `46/46` at `test-results/provider-usage-dashboard-e2e/2026-08-10T04-20-56-249Z`; typecheck, production build, durable inventory `13/13` and Effect required audit (`389` entries) pass. The running isolated preview is `dist-preview-provider-billing-api-20260810-122211/win-unpacked/CaoGen.exe`, SHA-256 `439B56EA0B60CA67F337782802514395722B6DC4CE4A340CDA47A80D3B12B9A3`; first-launch/restart diagnostics pass at `test-results/windows-unpacked-renderer-smoke/2026-08-10T04-26-11-454Z`. Real paid-account endpoints, cross-month accuracy, clean-candidate binding and final 1.0 acceptance remain open.
- 2026-08-10 local Provider Gateway increment (dirty worktree): a loopback-only OpenAI-compatible gateway now exposes health, model discovery, Chat Completions and Responses with encrypted gateway-token persistence, main-process-only token copying, one-time Provider credential leases, unique or explicit provider/model routing, redirect blocking, bounded bodies/concurrency/timeouts and backpressure-aware streaming. Requests reuse Provider circuit breakers, Key health and usage/pricing accounting; port conflicts fail closed and restart preserves the configured port and token. Provider usage `48/48`, Dashboard static `34/34`, real Electron Gateway `27/27`, Effect required audit (`393` entries), durable inventory (`87 modules / 13 checks`), typecheck, production build and two Windows cold starts pass. The running isolated preview is `dist-preview-provider-gateway-20260810-125854/win-unpacked/CaoGen.exe`, SHA-256 `8CA8AF035167AD51C2F586259418858C727A1C35E824E80BE3B73AD9B0F40B5F`; Gateway E2E report: `test-results/provider-gateway-e2e/2026-08-10T04-55-54-695Z`, renderer diagnostic: `test-results/windows-unpacked-renderer-smoke/2026-08-10T05-00-04-406Z`. Anthropic-to-OpenAI translation, Gemini native support, request-level cross-Provider Gateway failover, real paid-account/cross-month accuracy, clean candidate, installer, N1 and final 1.0 acceptance remain open.
- 2026-08-10 Anthropic Messages Gateway increment (dirty worktree): `/v1/messages` now translates system/text, constrained image blocks, stop sequences, tools/tool_choice, assistant tool_use and user tool_result into OpenAI Chat Completions, then translates non-stream and SSE responses back to Anthropic Message events. Thinking/top_k/document/MCP fields fail before Provider egress; upstream errors become bounded Anthropic envelopes without response-body leakage. Real Electron Gateway `35/35`, including x-api-key, image mapping, tool round-trip, stream order, error sanitization and no-egress rejection; Provider usage `48/48`, Dashboard `34/34`, Effect required (`393` entries), durable inventory (`87 modules / 13 checks`), typecheck and production build pass. The running isolated preview is `dist-preview-provider-anthropic-gateway-20260810-1318/win-unpacked/CaoGen.exe`, SHA-256 `B382BE3C86AD07604843B7EA6206F10B71CF6C4832594A4EEA6FE44388FA0676`; renderer diagnostic: `test-results/windows-unpacked-renderer-smoke/2026-08-10T05-18-08-741Z`. Native Anthropic thinking/cache parity, Gemini native support, request-level cross-Provider Gateway failover, real paid-account/cross-month accuracy, clean candidate, installer, N1 and final 1.0 acceptance remain open.
- 2026-08-10 native Anthropic runtime increment (dirty worktree): typed Provider settings now drive native `max_tokens`, temperature/top_p/top_k, Thinking disabled/adaptive/enabled with budget/display, and Prompt Cache automatic/system/tools/last-user with 5m/1h TTL. Structured settings override legacy request-body fields while preserving compatibility; request construction is immutable, and enabled Thinking fails closed when replay signatures are unavailable. Messages `19/19`, Tool Loop `10/10 + 1 Windows symlink environment skip`, Failover `10/10`, advanced config/editor `43/43 + 26/26`, request overrides `17/17`, real Electron runtime UI `8/8`, typecheck and production build pass. Coding-standards required remains failed on 37 earlier dirty-worktree/baseline-refresh items; no baseline was refreshed, and this increment's three new hard-limit regressions were removed. The running isolated preview is `dist-preview-provider-native-anthropic-20260810-1430/win-unpacked/CaoGen.exe`, SHA-256 `6C7E9DD70DB5CC62EAEFF8CBA049101C39FA031BB588BE5A4ACBD942211BA391`; renderer diagnostic: `test-results/windows-unpacked-renderer-smoke/2026-08-10T06-29-51-974Z`. Full multimodal parity, real paid-account handoff, Gemini native support, Gateway request-level cross-Provider failover, clean candidate and final 1.0 acceptance remain open.
- 2026-08-10 native Gemini runtime increment (dirty worktree): Google Generative Language now runs as its own Engine/Adapter with `x-goog-api-key`, system/text/image, functionCall/functionResponse, SSE/JSON, usage/pricing and typed `topK`/Thinking configuration. Thought and function signatures survive streaming aggregation, durable transcript replay, tool loops and Electron restart; missing or changing signatures fail closed. Contract smoke passes `7/7`; isolated real Electron local-mock E2E passes `6/6`; the unified Adapter parity gate passes OpenAI, Anthropic and Gemini in one run. The isolated unpacked preview is `dist-preview-provider-native-gemini-20260810-163024/win-unpacked/CaoGen.exe`, SHA-256 `B1483B0177D16ADFEBED35E5524054BA735F8CDF970FA8C8728D89D722417E4D`, Authenticode `NotSigned`, with no stable update metadata; first launch/restart renderer diagnostic passes at `test-results/windows-unpacked-renderer-smoke/2026-08-10T08-37-48-757Z`. No real Google account was called, so paid-account parity, complete multimodal/model capability coverage, billing accuracy, clean candidate and final 1.0 remain open.
- 2026-08-10 Gemini native Gateway increment (dirty worktree): the loopback Gateway now exposes Google Generative Language `/v1beta/models`, `:generateContent` and `:streamGenerateContent?alt=sse` in addition to its OpenAI and Anthropic surfaces. Google JSON/SSE bodies remain native, including inlineData, functionCall/functionResponse and thoughtSignature; the client `x-goog-api-key` is consumed only as the local Gateway token and never forwarded, while the selected Gemini Provider credential is injected through its scoped lease. Plain ambiguous models fail closed and `provider-id/model` routes one exact Gemini Provider; redirects and upstream response text are rejected from public errors. Protocol catalogs remain isolated: `/v1/models` lists OpenAI routes while `/v1beta/models` lists Gemini routes. usageMetadata maps prompt, candidates, thoughts and cached-content tokens into the shared usage/pricing ledger. Settings shows separate OpenAI `/v1` and Google `/v1beta` Base URLs plus each model's protocol. Real Electron Gateway passes `46/46` at `test-results/provider-gateway-e2e/2026-08-10T10-17-01-756Z`; Provider usage passes `48/48`, Dashboard `34/34`, unified Adapter parity, typecheck and production build pass. The isolated unpacked preview is `dist-preview-provider-gemini-gateway-20260810-182859/win-unpacked/CaoGen.exe`, SHA-256 `7D6EE6F636D950A3A04A9AFFC79AAFF80F9A2C93C4FDE031213F8E3FFA3D8899`, Authenticode `NotSigned`, with no stable update metadata; first launch/restart diagnostics pass at `test-results/windows-unpacked-renderer-smoke/2026-08-10T10-29-38-186Z`. No real Google account was called; paid-account parity, cross-month billing accuracy, Gateway request-level cross-Provider failover, clean candidate and final 1.0 remain open.
- 2026-08-10 Gateway request-level failover increment (dirty worktree): the loopback Gateway now performs bounded same-engine/same-wire-protocol failover only before downstream headers or bytes are committed. It retries network/provider-timeout, 401/403, 429 and selected 5xx failures, while HTTP 400, redirects and post-first-byte stream failures never replay. Global failover settings are inherited, Provider overrides and `maxRetries` are enforced, and Provider-specific circuit-breaker policy is registered before each attempt. Every upstream attempt has an isolated credential lease and its own usage/pricing row linked by request ID, ordinal and predecessor, so failed-attempt usage is retained without duplicating the successful attempt. Real Electron Gateway passes `58/58` at `test-results/provider-gateway-e2e/2026-08-10T11-14-26-348Z`; Provider usage `48/48`, Dashboard `34/34`, timeout `15/15`, circuit breaker `11/11`, Gemini `7/7`, protocol boundary, typecheck and production build pass. The isolated unpacked preview is `dist-preview-provider-gateway-failover-20260810-191523/win-unpacked/CaoGen.exe`, SHA-256 `67260273C6CD3AED3437C7FD63D7EA26C37C25F406F29C0B8213A5AF0332E60F`, Authenticode `NotSigned`, with no stable update metadata; two cold renderer launches pass at `test-results/windows-unpacked-renderer-smoke/2026-08-10T11-16-50-066Z`. This used isolated mock Providers only; real paid-account calls, cross-month accuracy, clean candidate and final 1.0 remain open.
- 2026-08-10 local-compute startup recovery increment (dirty worktree): Assistant entry still performs only fixed-loopback discovery. On an explicit first-task submission, retry, or Settings action, CaoGen can locate an installed Ollama runtime from bounded platform locations or `PATH`, start it without a Shell/visible console, wait at most eight seconds, and continue the unchanged draft. Missing runtime, start failure, and missing model remain distinct bounded results; Assistant keeps non-technical wording while Settings retains precise recovery, and no credential/path enters Renderer or evidence. Real Electron running-zero-config passes `6/6`, missing/stopped recovery `2/2`, and zero-choice routing `12/12` at `test-results/local-compute-zero-config/2026-08-10T11-54-49-146Z`, `test-results/local-compute-runtime-recovery/2026-08-10T11-54-49-144Z`, and `test-results/routing-zero-choice/2026-08-10T11-54-49-145Z`. Local Provider parity, Provider presets, onboarding smoke, typecheck, and production build pass. Coding-standards required still fails on 44 earlier dirty-worktree/baseline-refresh items; this increment's new hard-limit regressions were removed and the baseline was not refreshed. A machine with neither a runtime nor a user credential still has no immediate trial compute, so `M2-T6`, clean candidate, N1, and final 1.0 remain open.
- 2026-08-10 local-compute preview binding: isolated unpacked EXE `dist-preview-local-compute-recovery-20260810-195945/win-unpacked/CaoGen.exe`, SHA-256 `55881D37143590483CB1BBBD003A78F3076CCDBD61D7532103E14D58959857C1`, 213,968,384 bytes, Authenticode `NotSigned`, with no stable update metadata. Two isolated cold launches pass in 1715 ms and 1158 ms at `test-results/windows-unpacked-renderer-smoke/2026-08-10T12-00-59-488Z`; the temporary user-data directory was removed. This is a development preview only and does not close installer, signing, clean-candidate, or release gates.
- 2026-08-10 Provider 401 diagnostic increment (dirty worktree): model-catalog failures now carry a typed, redacted diagnostic context that identifies the effective engine, generation protocol and path (`/v1/responses`, `/v1/chat/completions`, `/v1/messages`, or Gemini `v1beta`), whether the request used the form Key, saved active Key or no credential, and explicitly states that the probe did not send a billable generation request. Attempts remain path-only; no credential, response body, Provider origin or project path is added to the context. Existing connectivity, P0/P1/P2 contract, Provider Profile `155/155`, advanced config `43/43`, authorization `129/129`, billing `62/62`, typecheck and production build pass. The isolated Windows x64 unpacked preview is `dist-preview-provider-diagnostic-20260810-202941/win-unpacked/CaoGen.exe`, SHA-256 `3B8E9F9F4DA156EE9F5B2F257ED30F5DA359AA91A6B4728B0A9B65BEBC919F78`, Authenticode `NotSigned`, with no `latest.yml`/blockmap; first-launch/restart diagnostics pass at `test-results/windows-unpacked-renderer-smoke/2026-08-10T12-30-58-311Z`. This is a development preview only; real paid-account requests, clean candidate and final 1.0 remain open.
